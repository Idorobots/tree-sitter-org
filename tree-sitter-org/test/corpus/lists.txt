================
Unordered list with dashes
================
- Item one
- Item two
- Item three

---

(document
  (zeroth_section
    (plain_list
      (item
        first_line: (plain_text))
      (item
        first_line: (plain_text))
      (item
        first_line: (plain_text)))))

================
Unordered list with plus
================
+ First
+ Second

---

(document
  (zeroth_section
    (plain_list
      (item
        first_line: (plain_text))
      (item
        first_line: (plain_text)))))

================
Second list item starts with verbatim markup
================
- This is fine
- =NOT OK=

---

(document
  (zeroth_section
    (plain_list
      (item
        first_line: (plain_text))
      (item
        first_line: (verbatim
          body: (verbatim_content))))))

================
Checkbox item with verbatim containing inner equals
================
- [X] Stale replica read returns error with =read_replicas=strict= config.

---

(document
  (zeroth_section
    (plain_list
      (item
        checkbox: (checkbox)
        first_line: (plain_text)
        first_line: (verbatim
          body: (verbatim_content))
        first_line: (plain_text)))))

================
List item code markup followed by slash text
================
- Stream data type (~XADD~, ~XREAD~, consumer groups).
- Test
- ~EVAL~ / Lua scripting (subset — no global state, no network access).

---

(document
  (zeroth_section
    (plain_list
      (item
        first_line: (plain_text)
        first_line: (code
          body: (code_content))
        first_line: (plain_text)
        first_line: (code
          body: (code_content))
        first_line: (plain_text))
      (item
        first_line: (plain_text))
      (item
        first_line: (code
          body: (code_content))
        first_line: (plain_text)))))

================
List items with URLs ending in slash
================
- The tree-sitter project is at https://tree-sitter.github.io/tree-sitter
- See also https://www.gnu.org/software/emacs/

---

(document
  (zeroth_section
    (plain_list
      (item
        first_line: (plain_text))
      (item
        first_line: (plain_text)))))

================
List items with inline code and trailing whitespace
================
- ~org-todo-keywords-1~ 
- ~org-link-parameters~

---

(document
  (zeroth_section
    (plain_list
      (item
        first_line: (code
          body: (code_content)))
      (item
        first_line: (code
          body: (code_content))))))

================
Plus sign in list item text is not a new bullet
================
- =protoc= 3.21+ (for the binary protocol code generation).

---

(document
  (zeroth_section
    (plain_list
      (item
        first_line: (verbatim
          body: (verbatim_content))
        first_line: (plain_text)))))

================
Drawer-like tail stays plain text in list items
================
- DONE Core string, list, hash, set, sorted set operations. :feature:
- DONE Three-node cluster with consistent hashing. :feature:
- DONE LRU and TTL eviction policies. :feature:

---

(document
  (zeroth_section
    (plain_list
      (item
        first_line: (plain_text))
      (item
        first_line: (plain_text))
      (item
        first_line: (plain_text)))))

================
Drawer-like tails with inline verbatim in list items
================
- DONE Fixed critical bug: =LMOVE= with =source == destination= corrupted the list. :bugfix:
- DONE Added =OBJECT IDLETIME= command. :feature:
- DONE Documented all configuration keys in =docs/configuration.md=. :docs:

---

(document
  (zeroth_section
    (plain_list
      (item
        first_line: (plain_text)
        first_line: (verbatim
          body: (verbatim_content))
        first_line: (plain_text)
        first_line: (verbatim
          body: (verbatim_content))
        first_line: (plain_text))
      (item
        first_line: (plain_text)
        first_line: (verbatim
          body: (verbatim_content))
        first_line: (plain_text))
      (item
        first_line: (plain_text)
        first_line: (verbatim
          body: (verbatim_content))
        first_line: (plain_text)))))

================
List items keep greater-than comparisons and drawer-like tails
================
- DONE Fixed =SCAN= cursor wraparound on very large keyspaces (> 100M keys). :bugfix:
- DONE =ZADD GT/LT= options now correctly handle negative infinity scores. :bugfix:

---

(document
  (zeroth_section
    (plain_list
      (item
        first_line: (plain_text)
        first_line: (verbatim
          body: (verbatim_content))
        first_line: (plain_text))
      (item
        first_line: (plain_text)
        first_line: (verbatim
          body: (verbatim_content))
        first_line: (plain_text)))))

================
Horizontal rule after list
================
- List item

----- 

---

(document
  (zeroth_section
    (plain_list
      (item
        first_line: (plain_text)))
    (horizontal_rule)))

================
Multiple list items starting with inline markup
================
- *Plurals* (controversial): some style guides permit =the 1990's= -- others forbid it.
- *Omission in poetry*: =o'er=, ='twas=, =ne'er=.

---

(document
  (zeroth_section
    (plain_list
      (item
        first_line: (bold
          body: (plain_text))
        first_line: (plain_text)
        first_line: (verbatim
          body: (verbatim_content))
        first_line: (plain_text))
      (item
        first_line: (bold
          body: (plain_text))
        first_line: (plain_text)
        first_line: (verbatim
          body: (verbatim_content))
        first_line: (plain_text)
        first_line: (verbatim
          body: (verbatim_content))
        first_line: (plain_text)
        first_line: (verbatim
          body: (verbatim_content))
        first_line: (plain_text)))))

================
Drawer after list item parses separately
================
- List item
  :DRAWER:
  :test: 23
  :END:

---

(document
  (zeroth_section
    (plain_list
      (item
        first_line: (plain_text)))
    (drawer
      name: (drawer_name)
      body: (drawer_kv_line))))

================
Indented quote block after list item
================
** Items with sub-blocks

- Stock types and their properties:
  #+begin_quote
  A good stock is the foundation of French cooking - it cannot be rushed.
  Auguste Escoffier
  #+end_quote

---

(document
  (heading
    stars: (stars)
    title: (plain_text)
    body: (section
      (plain_list
        (item
          first_line: (plain_text)))
      (quote_block
        body: (paragraph
          (plain_text)
          (plain_text))))))

================
Ordered list with numbers
================
1. First
2. Second
3. Third

---

(document
  (zeroth_section
    (plain_list
      (item
        counter: (counter)
        first_line: (plain_text))
      (item
        counter: (counter)
        first_line: (plain_text))
      (item
        counter: (counter)
        first_line: (plain_text)))))

================
Ordered list counter-set cookie
================
5. [@5] fifth
6. sixth

---

(document
  (zeroth_section
    (plain_list
      (item
        counter: (counter)
        counter_set: (counter_set)
        first_line: (plain_text))
      (item
        counter: (counter)
        first_line: (plain_text)))))

================
Ordered list cookie with apostrophe text
================
5. [@5] Review the day's bookings and note dietary restrictions.

---

(document
  (zeroth_section
    (plain_list
      (item
        counter: (counter)
        counter_set: (counter_set)
        first_line: (plain_text)))))

================
List with checkboxes
================
- [ ] Unchecked
- [X] Checked
- [-] Partial

---

(document
  (zeroth_section
    (plain_list
      (item
        checkbox: (checkbox)
        first_line: (plain_text))
      (item
        checkbox: (checkbox)
        first_line: (plain_text))
      (item
        checkbox: (checkbox)
        first_line: (plain_text)))))

================
Nested list — flat with indent preserved
================
- Test
  - Nested one
  - Nested two
- Not nested

---

(document
  (zeroth_section
    (plain_list
      (item
        first_line: (plain_text))
      (item
        indent: (item_indent)
        first_line: (plain_text))
      (item
        indent: (item_indent)
        first_line: (plain_text))
      (item
        first_line: (plain_text)))))

================
Blank-line-separated list items
================
- First

- Second

---

(document
  (zeroth_section
    (plain_list
      (item
        first_line: (plain_text))
      (item
        first_line: (plain_text)))))

================
Indented numeric continuation lines stay paragraphs
================
- Days off
  28 days off accounted for (224 days a year working days, ~8 holidays as well)
- Compensation?
  45 EUR/h (80k per year for 224 worknig days).

---

(document
  (zeroth_section
    (plain_list
      (item
        first_line: (plain_text)))
    (paragraph
      (plain_text))
    (plain_list
      (item
        first_line: (plain_text)))
    (paragraph
      (plain_text))))
