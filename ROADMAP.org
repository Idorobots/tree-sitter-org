#+TITLE: A parser for Org Mode nodes
#+ARCHIVE: ./docs/plans/ROADMAP_ARCHIVE.org::

* DONE Repo setup
:LOGBOOK:
- State "DONE"       from              [2026-03-19 Thu 20:20]
:END:
** DONE README.org
:LOGBOOK:
- State "DONE"       from "TODO"       [2026-03-06 Fri 19:40]
:END:
Please create a simple README.org file that will diescribe the project a bit - it's a Tree-Sitter-based parser for the Emacs Org-Mode files, aiming for 100% feature coverage.

Make sure to include some caveats in the README.org file:
- Nested lists are parsed as items on the parent list, but include an indent for disambiguation.
- Malformed objects are usually treated as plaintext.

Please list the features that are explicitly not supported (mentioned in the docs directory, but please validate against the current state of the parser if that is the case still).

Make sure to link the ROADMAP.org file.

** DONE Docs update
:LOGBOOK:
- State "DONE"       from "TODO"       [2026-03-06 Fri 19:44]
:END:
Update the target syntax documentation to reflect the currently supported syntax, and remove the "Notes on scope Drift" from the README file.

** DONE GitHub Actions
:LOGBOOK:
- State "DONE"       from "TODO"       [2026-03-06 Fri 19:46]
:END:
Please add a GitHub Actions workflow scaffold that will automatically run the tests.

It should run the Tree Sitter corpus tests as the firs step, then generate the parser and run the check.py script on the examples/ directory files and the ROADMAP.org and README.org files.
Any failures should be detected and rejected.

** DONE LICENSE
:LOGBOOK:
- State "DONE"       from "TODO"       [2026-03-06 Fri 19:45]
:END:
Add a LICENSE file. Use a MIT license with copyright for "Kajetan Rzepecki <k@spartan.works>".
* DONE Formal-ish specification
:LOGBOOK:
- State "DONE"       from              [2026-03-19 Thu 20:20]
:END:
** DONE Official docs analysis
:LOGBOOK:
- State "DONE"       from "TODO"       [2026-02-26 Thu 23:02]
:END:
We will be building an Org Mode file parser that aims to support all Org Mode features (eventually). The goal is to do some research that will help decide the right technologies used to iplement the parser.

Please analyze the official Org Mode syntax reference documentation and write it down in docs/research/syntax.md in a more usable fashion. You can find the official documentation here: https://orgmode.org/worg/org-syntax.html
Focus on the various Org Mode objects that are describe there and their relations. Keep it semi-formal, as a reference for later.

You can use other sources to obtain knowledge about the format and syntax. For instance, official distribution sources: https://cgit.git.savannah.nongnu.org/cgit/org-mode.git

** DONE Target syntax specification
:LOGBOOK:
- State "DONE"       from "TODO"       [2026-02-26 Thu 23:15]
:END:
Org Mode format is pretty complex, so I'd like to target a functional subset of the syntax. Please create a file docs/research/target-syntax.md that will outline the target syntax, that:

- omits inline tasks
- omits arbitrary keywords (only supports =#+CAPTION=)
- omits latex environments
- omits latex fragments
- omits babel
- omits inline babel calls
- omits entities (=\cent= etc)
- omits macros
- omits statistics cookies
- omits subscript & superscript
- omits sexp timestamps

All other Org Mode-supported objects should also be supported in the target syntax.

Keep the target syntax description semi-formal and lean, but detailed. Outline the different objects supported and their relations.

** DONE Extra test files
:LOGBOOK:
- State "DONE"       from "TODO"       [2026-02-27 Fri 10:23]
:END:
Generate additional test files that will utilize the less commonly used features of Org Mode and place them in examples/ directory. I'd like to see multiple files that each use a distinct, lesser used features from the target syntax, as well as combinations of features.

Example files can range from very small (even empty) to multiple nodes.

Create at least one file that contains more than a 100 nodes of various nestings and complexities.

Use your creativity for the node contents, such as headings, tags, properties and bodies.

* DONE Grammar & parser implementation
:LOGBOOK:
- State "DONE"       from              [2026-03-19 Thu 20:20]
:END:
** DONE Formal grammar
:LOGBOOK:
- State "DONE"       from "TODO"       [2026-02-27 Fri 13:09]
:END:
Translate the @docs/research/target-syntax.md into a formal description of the target org syntax and place it in @docs/plans/syntax.md .
My preference is PEG - parsing expression grammars, but you can use any formalism that you deem fit to describe the target syntax (for instance BNF), keep in mind that the parser will be translated into code, specifically using tree-sitter.

This description will be used to build a parser for the grammar.

You can include the excluded features as explicit text rules. We might expand the parser with those features in the future.

*** COMMENT
This took so much time and effort on the AI part it's kinda surprising. It backtracked back and forth between PEG and tree-sitter specific grammar, realising that it's not really possible to define a grammar for org files. The AI was sure it was able to write the grammar file multiple times, but then realised that something was missing. Eventually, it realised "Actually, I'm overcomplicating this." and then proceeded on a rant on for a bit, and then decided it was done without actually creating the grammar file (likely running out of context during thinking and confabulating stuff). After reprompting and asking it to create the file, it did spit out something, but honestly, it wasn't any better than what the free Zen Pickle model spat out in 1 minute.

The prompt above probably needs to be rewritten to try to get the AI to approximate the target syntax, or write it iteratively, starting with main features and then extending the syntax with less commonly used stuff.

** DONE Tree-sitter parser plan
:LOGBOOK:
- State "DONE"       from "TODO"       [2026-02-27 Fri 13:36]
:END:
Build a parser based on =tree-sitter= (https://tree-sitter.github.io/tree-sitter/index.html) and the formal grammar description in @docs/plans/syntax.md.

The parser should preserve the source locations of all the parsed nodes, this property will be used for original content reconstruction. Please take special care to implement error recovery in the parser, we want descriptive parse error messages.
Don't worry about the usability of the parsers output objects, we will address the usability (mapping parsed objects to semantic nodes) in Python at a later point.

The parser will be embedded in Python at a later stage.
You can test the parser on sample org files found in the examples/ directory in addition to using unittests.

Write the implementation plan down to docs/plans/tree-sitter-parser-impl.md

** DONE Implement the tree-sitter parser
:LOGBOOK:
- State "DONE"       from "TODO"       [2026-03-06 Fri 18:01]
:END:
Create a stub project with tree-sitter-cli. The dependency is already installed.

Implement the plan from @docs/plans/tree-sitter-parser-impl.md file.

Make sure it properly implements the grammar defined in @docs/plans/syntax.md
*** COMMENT
This took a very long while, but eventually created a tree-sitter parser that parses something. According to the AI:

#+begin_quote
### Results
- **68/68 unit tests passing (100%)** — up from 55/64 at session start
- **30/58 example .org files parse error-free (52%)** — up from 22/58
#+end_quote

WIth just 28 files parsing with errors. Looking at the parse tree produced, it wasn't great - separate org table nodes for individual rows, etc.

*** DONE Timestamp hours
:LOGBOOK:
- State "DONE"       from "TODO"       [2026-02-28 Sat 14:05]
:END:
I see that timestamps that have a hour or a time range associated with them do not parse it:

: * DONE Recurring daily standup :Agile:GTD:
: SCHEDULED: <2026-02-28 Sat 10:00-12:00 +1d> DEADLINE: <2026-02-28 Sat 13:00> CLOSED: [2026-02-28 Sat 12:00]
: 
: Date [2025-10-10]
: Date day <2025-10-10 Mon>
: Date day time <2025-10-10 Mon 12:00>
: Date day timerange <2025-10-10 Mon 12:00-13:00>

All of these example timestamps parse fine, but the time/timerange component is not part of the parse tree.
Please adjust the parser so that this parses correctly.

*** DONE Planning with newlines
:LOGBOOK:
- State "DONE"       from "TODO"       [2026-02-28 Sat 14:12]
:END:
Planning only parses when all fields are on the same line, but in Org Mode they can be on separate lines:

: * DONE Recurring daily standup :Agile:GTD:
: SCHEDULED: <2026-02-28>
: DEADLINE: <2026-02-28>
: CLOSED: [2026-02-28]

This should still parse as one planning block with 3 keywords.
Please adjust the parser so that this parses correctly.

*** CANCELLED Nested lists
:LOGBOOK:
- State "CANCELLED"  from "TODO"       [2026-03-03 Tue 13:16] \\
  See comment.
:END:
Nested lists don't work very well:

: - Test
:   - Nested
:   - Nested
: - Not nested

This should result in a parse tree that looks something like this:

#+begin_example
    plain_list:
      item:
        first_line: plain_text
        plain_list:
          item:
            first_line: plain_text
          item:
            first_line: plain_text
      item:
        first_line: plain_text
#+end_example

But instead it's parsed as two separate lists with a paragraph inbetween.
Please adjust the parser so that this parses correctly.
**** COMMENT
Proposed to parse them as one list and handle the indented fields later on.
*** DONE Separate tables
:LOGBOOK:
- State "DONE"       from "TODO"       [2026-03-03 Tue 16:51]
:END:
Each row of a table parses as a separate org table:

: | Properly affiliated table | Column 2 |
: |---------------------------+----------|
: | Row                       | Data     |

The above example should parse as a single table with 3 rows. First and last rows should have two cells each, and the middle row should be a table rule. It seems like the row parsing is correct, but multiple consecutive rows are not merged into a single table.
Please adjust the parser & tests so that this parses correctly. It is OK to modify the grammar.

*** DONE Fixed-width section fix
:LOGBOOK:
- State "DONE"       from "TODO"       [2026-03-03 Tue 21:10]
:END:
Fixed-width section should only start with a collon preceeded by whitespace.

These are all valid:
#+begin_example
: This is OK
: Continuation
   : This is also OK
   : So is the following
:
   :
#+end_example

These should not parse as fixed-width sections but currently are:
#+begin_example
test :: Not a fixed width section
#+end_example

Additionally, a collon character within a paragraph, but preceeded by other non-whitespace characters should be treated as regular text, only collons preceeded by whitespace at the beginning of the line indicate a fixed-width section.

This should all be valid:
#+begin_example
Test paragraph:
Another test: yes, it psases
#+end_example

**** COMMENT
Claude "fixed" the issue, but broke some other tests in the process.

*** DONE Fixed-width slop
:LOGBOOK:
- State "DONE"       from "TODO"       [2026-03-03 Tue 21:10]
:END:
Looks like drawers and heading tags now do not work properly. Please make sure that the fixed-width section is on parsed when the =:= character is preceeded only by whitespace. Make sure that when there's any content following the fixed-width collon, there must be a whitespace character separating the collon from the content. Otherwise the line should be treated as a property.
Under no circumstances should a line with leading non-whitespace characters be treated as a fixed-width section.

*** DONE Inline markup fix
:LOGBOOK:
- State "DONE"       from "TODO"       [2026-03-03 Tue 18:46]
:END:
Inline markup doesn't parse as distinct objects, please define additional rules and make sure that inline markup objects are parsed correctly in paragraphs and heading titles.

*** DONE Inline markup strike-through
:LOGBOOK:
- State "DONE"       from "TODO"       [2026-03-03 Tue 21:16]
:END:
The inline strike-through markup doesn't work at the begining of the line and in titles it seems.
All of the following should parse fine and result in a strike-through object to be parsed:

#+begin_example
This should be +strike-through+
+so is this+
+And also+ this
#+end_example

Heading title examples:

: * Heading +strike+
: * +strike throguh+ yeah
: * TODO  +strike throguh+

*** DONE Text coalescence
:LOGBOOK:
- State "DONE"       from "TODO"       [2026-03-04 Wed 00:21]
:END:
The =-= character in the title/paragraph should not split the text.

This currently parses as a heading with a title consisting of three distinct parts =Fixed=, =-= and =Width Area=:

: * Fixed-Width Areas

Instead, the title should parse as a single plain text item.

*** DONE Node properties without value
:LOGBOOK:
- State "DONE"       from "TODO"       [2026-03-03 Tue 18:22]
:END:
Node properties don't bind the values in a property drawer - that is, =node_property= should have a =property_name= and a =property_value=.

*** DONE Blocks body
:LOGBOOK:
- State "DONE"       from "TODO"       [2026-03-03 Tue 21:57]
:END:

Looks like the =example_block= doesn't bind the contents in the parse tree, please bind it the same way that =quote_block= does.
The =verse_block= binds the body in a different way (without a paragraph) please adjust that to match the other blocks.

*** DONE Example body again
:LOGBOOK:
- State "DONE"       from "TODO"       [2026-03-03 Tue 22:01]
:END:
This should parse and preserve the indentation:

: #+begin_example
: verbatim content
:   preserved indentation
: #+end_example

I expect the block body to have two plaintext items.

*** DONE Special keywords don't bind their values
:LOGBOOK:
- State "DONE"       from "TODO"       [2026-03-03 Tue 22:15]
:END:

Looks like the zeroth section special keywords aren't binding their values. I expect =special_keyword= to have a =keyword_key= and a =keyword_value=, much like the node properties.

Furthermore, a special keyword within a heading seems to be parsed as a =comment= followed by a =paragraph=. I would like these to be parsed as special keywords.

: # Comment in zeroth section
: #+TODO: SPECIAL KEYWORD
: * Heading
: #+TODO: This is actually treated as a comment

This should result in a comment, then a special keyword, then a heading with a special keyword.

**** COMMENT
Codex decided that only #+TODO is a comment likely not to break #+CAPTION, so I decided it should parse all special keywords everywhere.

*** DONE The src block body
:LOGBOOK:
- State "DONE"       from "TODO"       [2026-03-03 Tue 22:23]
:END:
The =src_block= doesn't bind its body in the parse tree. I'd like it to bind the body and also the configuration switches that go next to the language.

Given the following block:
: #+begin_src python -n
: x = 1
: y = 2
: z = x + y
: #+end_src

The expected parse tree is an =src_block= that binds "python" as the =language=, "-n" as the =arguments= and the contents of the block as =body=.

*** DONE More table fixes
:LOGBOOK:
- State "DONE"       from "TODO"       [2026-03-03 Tue 22:39]
:END:
Indented tables don't seem to parse correctly.
Furthermore, an indented table with a =#+CAPTION:= in front of it fails to parse.

Both of these should parse correctly (note the leading two space indent):

:   #+CAPTION: A small reference table
:   | Key | Value |
:   |-----+-------|
:   | a   |     1 |
:   | b   |     2 |

Table with 4 rows, two columns, with a rule on the second row and a caption.

:   | Item    |
:   |---------|
:   | apple   |
:   | banana  |
:   | cherry  |

Table with 5 rows, each one cell. With a rule on the second row:

It is OK to skip the leading whitespace, but do include it in the table object.

*** DONE Even more table fixes
:LOGBOOK:
- State "DONE"       from "TODO"       [2026-03-03 Tue 23:54]
:END:
There is an issue when parsing tables within heading. You can find a reproduction of the issue in the @examples/table-basic.org file. All tables in that file should parse without any issues.
Please check it out and determine the root cause.

*** DONE Clock support
:LOGBOOK:
- State "DONE"       from "TODO"       [2026-03-04 Wed 09:40]
:END:
Clock lines don't parse correctly currently.

Please check the @examples/clock-basic.org for example that should parse correctly.

*** DONE Collon in heading title
:LOGBOOK:
- State "DONE"       from "TODO"       [2026-03-04 Wed 10:18]
:END:
There is as aissue when parsing a heading tittle with a =:= character in in:

: * TODO Try to debug the ejabberd_tls:send timouts.                :Debugging:Erlang:XMPP:

The parser is reporting an error before the =:= character followed by a single tag =send= which is incomplete. That character should be parsed as part of the title with tags starting at =Debugging=.

*** DONE More text coalescing
:LOGBOOK:
- State "DONE"       from "TODO"       [2026-03-04 Wed 10:39]
:END:
The =_= and =:= characters in the title/paragraph should not split the text.

This currently parses as a heading with a title consisting of several distinct parts =Fixed=, =_=, =Width=, =:= and =Area=:

: * Fixed_Width: Areas

Instead, the title should parse as a single plain text item.

There are likely other such "special characters" that do have a semantic meaning when wrapping a portion of text.
If the title is missing such a closing character, the initial one should always be parsed as part of the text.
A similar treatment should be done to these characters & text within paragraphs.

*** DONE Property & drawer fixes
:LOGBOOK:
- State "DONE"       from "TODO"       [2026-03-04 Wed 12:28]
:END:
There are some cases regarding drawers that don't work very well:

1. Whitespace in properties drawer:

: :properties:
:
: :end:

This parses with an error in the body of the drawer. It does work for other drawer types. I'd like an empty body of a properties drawer to be handled more gracefully.

2. Empty property values (just whitespace) break the parser:

: :properties:
: :key: value
: :context: 
: :some_key: value
: :end:

Ideally, these would be handled more gracefully (a property with an empty value).

3. The =:= character in property values breaking the parser:

: :properties:
: :field: value
: :some_key: value:test
: :end:

Collons should be treated as text in the property value.

4. Drawers in the body of list items do not parse at all:

: - List item
:   :drawer:
:   :test: 23
:   :end:

There are more issues with the list bodies, so for now it is OK if these are parsed as separate drawers, not within the body of the list item.

5. Indented drawers don't parse very well in general (note the 2-space indent on all of these):

:   :drawer:
:   :end:

Indicates an error in the whitespace before both the drawer name and the end.

:   :properties:
:   :key: ~/test/value
:   :end:

This parses as a drawer when in the zeroth section, but reports errors within it, while not parsing as a drawer at all when in a heading.
Drawers should be indentable - the leading whitespace can be ignored for the drawer name, end line and each =:key:=, but should be retained for the textula drawer contents.

6. Custom drawers with key-like values in them don't parse:

: :drawer:
: :test:
: :end:

This should parse without an error and bind the body of the drawer.

: :drawer:
: :test: value
: :end:

This should also parse and bind the body of the drawer.

*** DONE Collon fixes
:LOGBOOK:
- State "DONE"       from "TODO"       [2026-03-04 Wed 12:34]
:END:
The collon character still breaks some of the parsing. Here are some examples that should parse without issues:

: :drawer:
: 2025-01-12 :: foo bar
: :end:

Similar error due to the =:= character.

: - some list item: oops

Again, problem with the collon character.

*** DONE Text fixes
:LOGBOOK:
- State "DONE"       from "TODO"       [2026-03-04 Wed 13:12]
:END:
Plain text objects sometimes don't accept any unicode characters and report errors when these are encountered. Please make sure the parser recognizes unicode characters as text. Here are some examples:

#+begin_example
body → unit
Zażółć gęślą jaźń.
O(n²)
#+end_example

In a similar vein, the =>= and =<= alone on a line seem to break parsing and causes =plain_text= to be split into multiple objects. These should be treated as regular text and not break the text into chunks. All of the following should be parsed as text lines:

#+begin_example
Test > hurr
Durr < derp
Foo [ bar
Bar ] baz
#+end_example

The =-= character still sometimes breaks the parsing:

#+begin_example
The earliest printed books, incunabula, often had no punctuation at all,
leaving spaces for readers or scribes to add marks by hand. Gutenberg's
42-line Bible c. 1455) is a notable early example.
#+end_example

The =-= on the final line causes a parsing error, but should be treated as just text.

A similar issue exists for the =@= character:
#+begin_example
Report issues to mailto:bugs@example.org or via the issue tracker.
#+end_example

The parser breaks on the first line due to the =@= character.

*** DONE Markup fixes
:LOGBOOK:
- State "DONE"       from "TODO"       [2026-03-04 Wed 14:21]
:END:
There are some issues with the inline markup:

1. Inline markup fails to parse in a caption, when embedded in a heading body. Seems to work fine outside:

: #+CAPTION: *hurr* - durr derp
: works
: 
: * Heading
: #+CAPTION: *hurr* - durr derp
: doesn't work

Both of these should be parsed the same way.

2. Markup characters alone in a caption are causing an error:

#+begin_example
#+CAPTION: *_?=
foo
#+end_example

The caption value should parse as just text.

3. Inline markup edge cases to handle:
#+begin_example
=Foo = bar=
#+end_example

This should be recognized as a singe verbatim section, since the ~=~ character is preceeded by whitespace in side of the value.

4. Inline markup in list items doesn't seem to parse very well:

#+begin_example
- *Plurals* (controversial): some style guides permit =the 1990's= — others forbid it.
- *Omission in poetry*: =o'er=, ='twas=, =ne'er=.
#+end_example

Both of these lines should parse without any issues and recognize the bold and verbatim sections.

**** COMMENT
Codex tried fixing the issues, but then reverted all changes as too many regressions were introduced.
It did fix the verbatim case alone and then fixed the CAPTION.

*** DONE Cite fixes
:LOGBOOK:
- State "DONE"       from "TODO"       [2026-03-04 Wed 14:46]
:END:
There are some edge cases regarding the citation parsing.

All of these lines should parse with a citation recognized within them delimited by the =[= and =]= characters.

#+begin_example
In prose: the foundational theory of information [cite:@shannon1948] was

- Lambda calculus [cite:@church1936] — the foundation of functional programming.

| 1936 | Church               | [cite:@church1936]     | Lambda calculus            |


[cite/a:@knuth1968]
[cite/numeric/brackets:@hopcroft1979]

[cite: see;@martin2009]
[cite: e.g.;@liskov1987;@meyer1988]
[cite: for a contrasting view, see;@wirth1974]

[cite: see also;see @chomsky1956 §4;cf. @bar-hillel1961 pp. 91-92;@ginsburg1966]

[cite: as argued by *three independent groups* almost simultaneously,;@vaswani2017;@gehring2017;@wu2016]
#+end_example

*** DONE Block fixes
:LOGBOOK:
- State "DONE"       from "TODO"       [2026-03-04 Wed 15:11]
:END:
There are some more edge cases when parsing blocks:

1. Comma-quoted contents:

: #+begin_example
: ,* This line starts with an asterisk — comma-quoted.
: ,** So does this — also comma-quoted.
: 
: Normal text needs no quoting.
: 
: ,#+TITLE: This #+KEY line is also comma-quoted (optional for safety).
: #+TITLE: This one is not quoted
: #+end_example

I'd like the parser to not make an attempt at stripping the commas, but it should recognize the whole body of the example without any errors.

2. Some characters in the body of an example block cause parse issues:

: #+begin_example
: Receiving objects: 100% (1842/1842), 3.21 MiB | 4.5 MiB/s, done.
: #+end_example

Here, the =|= character causess an issue.  The example block body should not be parsed for objects, the body should always be text.

3. Custom dynamic blocks fail to parse when there are multiple instances one after another:

: #+begin: clocktable
: | Bibliography check   | 0:30   |
: #+end:
: 
: #+begin: columnview
: | Second chapter           | TODO | B        | :writing:  |
: #+end:

Here, the second block's body is not parsed correctly and results in an error. The parsing state should be cleared when the custom dynamic block is finished parsing so that both of these objects are treated separately and parsed correctly. The example works if there is a line of text inserted between the blocks.

4. Looks like the issue still exists for the dynamic blocks - see the @examples/dynamic-blocks.org file for a reproduction

*** DONE Even more inline markup fixes
:LOGBOOK:
- State "DONE"       from "TODO"       [2026-03-04 Wed 17:20]
:END:
The following example parses as a long verbatim markup followed by ~;=~, but there should be two separate verbatim values: ~=REFERENCES=~ and ~=;=~ surrounding some plain text.

#+begin_example
=REFERENCES= contains one or more citation reference objects separated by =;=.
#+end_example

Similar issue happens here:

#+begin_example
Note comma-quoting in example block above: lines that would start with =,*= (like =,* First Heading=) are comma-quoted.
#+end_example

Here' the ~=,*= (like =,* First Heading=~ is parsed as ~=,*= (like =~ and ~,* First Heading=~ resulting in an error. Instead, it should be parsed as ~=,*=~ and ~=,* First Heading=~ separated by some text.

*** DONE Lists fixes
:LOGBOOK:
- State "DONE"       from "TODO"       [2026-03-04 Wed 17:39]
:END:
There are some list edge cases:

1. Inline markup in list items

#+begin_example
- ~org-todo-keywords-1~ 
- ~org-link-parameters~
#+end_example

The first list item is not prarsed properly because of the trailing whitespace. Once removed, or if anything is added past the trailing whitespace it starts working again.
The second list item is not parsed properly because of an error related to the inline code.

A more involved example follows:

#+begin_example
- *Plurals* (controversial): some style guides permit =the 1990's= — others forbid it.
- *Omission in poetry*: =o'er=, ='twas=, =ne'er=.
#+end_example

Both of these examples should parse without any issues and recognize the bold, verbatim and code sections.

2. Horizontal rule following a list doesn't parse:
#+begin_example
- List item

-----
#+end_example

The horizontal rule should be parsed as a separate object.

*** DONE Special characters
:LOGBOOK:
- State "DONE"       from "TODO"       [2026-03-04 Wed 18:23]
:END:
There are some "special character" issues.

1. The =#= character break title parsing:
#+begin_example
,* REVIEWING Code review: PR #412 :review:backend:
#+end_example
It should be treated as a regural text character.

2. The =reguar_link= description fails to parse when it contains =/= characters
#+begin_example
[[https://orgmode.org][See also https://orgmode.org/worg/ for community docs]]
#+end_example

Another place where this breaks is:
#+begin_example
- The tree-sitter project is at https://tree-sitter.github.io/tree-sitter
- See also https://www.gnu.org/software/emacs/
#+end_example

Here, first line parses fine, but the second errors out.

*** DONE Indents
:LOGBOOK:
- State "DONE"       from "TODO"       [2026-03-04 Wed 18:33]
:END:
Indents in the custom drawers fail when the body contains a timestamp:

#+begin_example
:PROGRESS:
  [2025-01-06 Mon] 12,400 documents collected. Deduplication in progress.
:END:
#+end_example

This is also the case when all parts of a drawer are indented:
#+begin_example
  :PROGRESS:
  [2025-01-06 Mon] 12,400 documents collected. Deduplication in progress.
  :END:
#+end_example

*** DONE Further markup edge cases
:LOGBOOK:
- State "DONE"       from "TODO"       [2026-03-04 Wed 19:02]
:END:
I found some more markup-related issues.

1. Here the two inline markup objects are separated by a =/= character reporting multiple errors.
#+begin_example
Increment hash field values atomically — identical semantics to ~INCR~/~INCRBYFLOAT~.
#+end_example

A similar situation happens in tables:

#+begin_example
| =[persistence]=| ~aof_enabled~       | bool    | =false=        |
| =[persistence]=| ~aof_fsync~         | enum    | =everysec=     |
#+end_example

Here twe =|= character follows the ~=~ character causing an error to be reported for the code markup. Adding a space makes it work again.

It should also OK if there is nothing between the different markup objects:

#+begin_example
*Bold*/italic/ — two markup objects sharing a boundary.
~code~=verbatim= — code immediately followed by verbatim.
#+end_example

All four markup objects should parse correctly here.

2. Some opening markup objects should be supported. Markup can also nest:

#+begin_example
- */Bold and italic simultaneously/* — achieved by nesting one inside the other.
- /*Italic wrapping bold*/ — the outer marker determines the outer type.
- *A _bold underlined_ phrase* — underline nested inside bold.
- /An _italic underlined_ passage/ — underline nested inside italic.
- (*/nested bold-italic inside parens/*) — opens with =(= which is a valid PRE.
- "/italic inside double quotes/" — double quote is a valid PRE.
- '/italic inside single quotes/' — single quote is a valid PRE.
#+end_example

All of these should parse without errors and have the requisite markup present.

3. Analogically, sometimes a markup character should be treated as plain text, when they don't delineate an object without whitespace:

#+begin_example
- = not verbatim = (space before closing)
- * not bold * (space before closing)
- / not italic / (space before closing)
#+end_example

All of the above should parse as plain text without any markup.

*** DONE Lists handling
:LOGBOOK:
- State "DONE"       from "TODO"       [2026-03-04 Wed 22:13]
:END:
1. The markup on the second list item doesn't parse correctly:
#+begin_example
- This is fine
- =NOT OK=
#+end_example

Another example:
#+begin_example
- [X] Stale replica read returns error with =read_replicas=strict= config.
#+end_example

Another example of weird markup behaviour:
#+begin_example
- Stream data type (~XADD~, ~XREAD~, consumer groups).
- Test
- ~EVAL~ / Lua scripting (subset — no global state, no network access).
#+end_example

Last line reports numerous errors.

2. There are cases where lists are parsed in contexts they don't appear in, like tables:
#+begin_example
| CPU      | 4 vCPU   | 8+ vCPU     |
| RAM      | 8 GiB    | 32+ GiB     |
| Storage  | 20 GiB   | 100+ GiB NVMe |
| Network  | 1 Gbps   | 10 Gbps     |
#+end_example

This reports an error, because of the =+= characters interpreted as the bullets on a list.

A very similar issue happens for lists too:

#+begin_example
- =protoc= 3.21+ (for the binary protocol code generation).
#+end_example

Here the =+= is interpreted as a start of a new list item, but it should be just text.

3. Drawer-like things don't parse in list items:

#+begin_example
- DONE Core string, list, hash, set, sorted set operations. :feature:
- DONE Three-node cluster with consistent hashing. :feature:
- DONE LRU and TTL eviction policies. :feature:
#+end_example

In each one of these, the =:features:= part should be parsed as plaintext.

4. List item cookies don't parse correctly:

#+begin_example
5. [@5] fifth
6. sixth
#+end_example

Here, the first line contains a cookie that sets the counter. It should be parsed correctly.

Similarly:

#+begin_example
5. [@5] Review the day's bookings and note dietary restrictions.
#+end_example

The parser complains about the ='= character because of the cookie issue earlier.

5. Some unicode characters are not supported when usig list cookies:
#+begin_example
1. [@1] Confirm gelato is at scooping temperature (-11°C).
#+end_example

Here the =°= character should be treated as text, but isn't due to the cookie error.

*** DONE Head line
:LOGBOOK:
- State "DONE"       from "TODO"       [2026-03-04 Wed 23:08]
:END:
1. When a TODO state is not recognized but followed by the priority, the parser returns an error:

#+begin_example
,* PREPRODUCTION [#B] Salt and Longitude :thriller:contemporary:
#+end_example

Instead, the priority should be treated as plain text in such a case. We'll handle the recognition of custom TODO-items separately.

2. Markup characters can throw off the title parsing:

#+begin_example
,** Restart repeater (.+) — reschedule N units from the completion date
#+end_example

The =+= character in this case seems to cause a parsing failure of the title. The whole line should be parsed as a heading with just plaintext title.

*** DONE Parser configuration / dynamic changes
:LOGBOOK:
- State "DONE"       from "TODO"       [2026-03-04 Wed 19:17]
:END:

The =#+TODO:= special key should be supported. I.e. When encountered by the parser, it should take effect for the following file contents.
The supported forms should include:
#+begin_example
,#+TODO: TODO | DONE
,#+TODO: SUSPENDED
,#+TODO: WORKING |
,#+TODO: TODO(t) | DONE(d@/!)
,#+TODO: | CANCELLED(c@/!) REWORKED(r@/!)
#+end_example

That is, plain definitions with todo/done separator. Just plain definition, empty done keys, empty todo keys, extra suffix with the org-mode controls (can be ignored).

=#+TODO:= lines should just add keys to the set, never replate the existing set. When there are multiple lines, treat all of the keys as valid keys.

*** DONE More edge cases
:LOGBOOK:
- State "DONE"       from "TODO"       [2026-03-04 Wed 23:50]
:END:
There are still some edge cases present:

1. Markup with hyphens and parens doesn't parse correctly:
#+begin_example
_Underlined_ text should be used sparingly to avoid confusion with hyperlinks.
Consider: (_underline inside parentheses_) and -_underline after a hyphen_-.
#+end_example

In this case we expect three separate underline objects in those two lines.

2. Indented quote block doesn't parse right:
: ** Items with sub-blocks
: 
: - Stock types and their properties:
:   #+begin_quote
:   A good stock is the foundation of French cooking — it cannot be rushed.
:   — Auguste Escoffier
:   #+end_quote

Please treat the indent as optional, much like in the example blocks. The same treatment should be applied to all blocks.

3. Lists with drawer-like contents still parse wrong:

#+begin_example
- DONE Fixed critical bug: =LMOVE= with =source == destination= corrupted the list. :bugfix:
- DONE Added =OBJECT IDLETIME= command. :feature:
- DONE Documented all configuration keys in =docs/configuration.md=. :docs:
#+end_example

All of these lines should contain a list item with one verbose inline object surrounded by text. The text should include the =:text:= strings.

Another example:

#+begin_example
- DONE Fixed =SCAN= cursor wraparound on very large keyspaces (> 100M keys). :bugfix:
- DONE =ZADD GT/LT= options now correctly handle negative infinity scores. :bugfix:
#+end_example

*** DONE Multiline inline objects
:LOGBOOK:
- State "DONE"       from "TODO"       [2026-03-05 Thu 00:07]
:END:
We can now address the multiline inline markup. Specifically footnotes, citations and links:

1. Inline footnotes that span multiple lines not parsed:
#+begin_example
An inline footnote with a very short definition: the slash is called a
solidus[fn:solidus:From Latin /solidus/, a gold coin whose abbreviation /s/ was
written in the numerator position of the price fraction.] in formal contexts.
#+end_example

The footnote should be delimited by =[= and =]= even when spanning multiple lines.

2. Multi-line citations don't work:
#+begin_example
The question of whether neural networks can learn formal grammars has been
studied extensively [cite: see the surveys in;@chomsky1956;@gold1967;@angluin1987;
see also recent work on;@bhattamishra2020;@hahn2020; for transformer-specific results].
#+end_example

The citation should be delimited by =[= and =]= even when spanning multiple lines.

3. Multi-line links are not supported:
#+begin_example
<https://very-long-domain-name.example.com/with/a/very/long/
path/that/spans/multiple/lines/for/testing/purposes>
#+end_example

The link should be delimited by =<= and =>= even when spanning multiple lines.

*** DONE Indented logbook
:LOGBOOK:
- State "DONE"       from "TODO"       [2026-03-05 Thu 09:14]
:END:
The indented LOGBOOK drawer seems to not be parsed correctly. I prepared a reproduction in @examples/repeats.org
Looks like the fix broke @examples/tables.org in multiple places.

*** DONE List item indents
:LOGBOOK:
- State "DONE"       from "TODO"       [2026-03-05 Thu 11:09]
:END:
Implement indent handling for list items nesting. The parser should stay flat but recognize the indent level on items within a list so that a post-processing step can recover the nesting structure.
All objects that can be placed under a list item should be handled, drawers, text, other lists, etc.
There is no need to implement the post-processing step just yet.

There are some issues with the implementation:
#+begin_example
- Days off
  28 days off accounted for (224 days a year working days, ~8 holidays as well)
- Compensation?
  45 EUR/h (80k per year for 224 worknig days).
#+end_example

The indent is recognized properly, but the text isn't - when the indent is removed the text on these list items is recognized as a paragraph. Ideally, the indent would be attached to the paragraph, so that the list structure can be recovered at a later phase.

*** DONE List counters
:LOGBOOK:
- State "DONE"       from "TODO"       [2026-03-05 Thu 11:32]
:END:
Lists and headings should recongnize counters:

#+begin_example
,* List [0/3]
- [-] Sublist [1/2]
  + [ ] Unfinished
  + [X] Finished
- [-] Todo with an empty counter [/]
- [ ] Another item, no counter
#+end_example

The =[= and =]= characters delineate a completion counter for the lists. The counter can be empty =[/]=, it can also have a set value =[1/4]=. It must contain the slash character to be treated as a counter. It can coexist with the priority cookie on the heading:

#+begin_example
,* TODO [#B] [/] Priority and counter
#+end_example

Please add counter handling to both the list items and also the heading title.
Please also include the percentage cookies.

Add a few more corpus test cases:
#+begin_example
,* TODO [#A] [%] Priority and empty counter
,* TODO [#A] [20%] Priority and counter
,* TODO [#B] [2/10] Priority and counter
,* TODO [#C] Priority and counter [10/10]
,* TODO No priority [1/5] and counter in the middle
- [%] Empty percentage
- Empty percentage at the end [%]
- Set percentage [21%]
- Set [33%] percentage
- [/] Empty count
- Empty count at the end [/]
- Set count [1/6]
- Set [4/6] count
#+end_example

All of these should parse without errors.

*** DONE Support additional special keywords
:LOGBOOK:
- State "DONE"       from "TODO"       [2026-03-05 Thu 12:16]
:END:
Add support for the following special keywords:
- =#+TBLNAME:= - should associate to the next object, much like caption
- =#+RESULTS:= - should associate to the next object, much like caption
- =#+PLOT:= - should associate to the next object, much like caption

I found an edge case in handling of =RESULTS= keyword:

#+begin_example
|------------+----------+--------+--------------+---------+-----------+---------------+-----------+------------+-----------|
|     *Date* | *Stocks* | *Cash* | *Currencies* | *Bonds* |  *Metals* | *Real estate* |  *Liquid* | *Movables* |   *Total* |
|------------+----------+--------+--------------+---------+-----------+---------------+-----------+------------+-----------|

#+RESULTS:
|------------+--------------------+--------------------+--------------------+--------------------+--------------------+---------------|
|     *Date* |           *Stocks* |             *Cash* |       *Currencies* |            *Bonds* |           *Metals* | *Real estate* |
|------------+--------------------+--------------------+--------------------+--------------------+--------------------+---------------|
#+end_example

The table following the RESULTS keyword is misparsed.

*** DONE Link-related edge cases
:LOGBOOK:
- State "DONE"       from "TODO"       [2026-03-05 Thu 13:47]
:END:
I found some edge cases when handling links:

1. Text Links with an ampersand in them cause parser errors:

#+begin_example
OTA updates: https://fota.pintracview.com/fota/T9/check_update.php?carrier=<CARRIER>&rev=<CURRENTVERSION>&imei=<IMEI>
#+end_example

Could be caused by text blocks not accepting the ampersand character.

2. Emails in angle bracket links cause a parser failure:

#+begin_example
,* Emails
test@example.com,
Test <test@example.com>,
Test <mailto:test@example.com>,
#+end_example

Might be caused by angle links requiring the protocol schema as the third line seems to work fine.

3. Links in headlines:

#+begin_example
,*** TODO [[definitely][durchaus]], [[according to][zufolge (+dat.)]], [[according to][laut (+dat.)]]
#+end_example

The first link parses fine, but the remainder do not parse at all and cause an error. Seems to be caused by the markup-like characters (=(=, =)= and =+=) )in the link description.

4. Here's another failing case:

#+begin_example
"Test. Test | Test" <test.test-test@example.com>,
#+end_example

*** DONE More and more inline markup edge cases
:LOGBOOK:
- State "DONE"       from "TODO"       [2026-03-05 Thu 15:21]
:END:
Here are some examples of inline-markup that don't parse correctly:

#+begin_example
- Line
- =map= - takes a function =A => B=
- =apply= (=ap=) - applies a function =F[A => B]=
- =pure= - wraps the value into the type constructor: =A => F[A]=,
- Essentially combines several =D => F[A*]= in a chain, so that you get a single =Kleisli[F, D, R]= result at the end which you can then =run= with the dependency =D=.
#+end_example

It seems that in all cases the ~=>~ sequence is throwing the parser off.
*** DONE List parsing edge cases
:LOGBOOK:
- State "DONE"       from "TODO"       [2026-03-05 Thu 14:52]
:END:
1. I found one more case that fails the list parsing

#+begin_example
 - React + Redux
 - Elm sucks
 - Reframe on CLJS
#+end_example

The =+= character is treated as a start of the list, but it shouldn't.

2. Another edge case is when the paragraph following a list item starts with a bullet, but isn't a list itself:

#+begin_example
- State "CANCELLED"  from "TODO"       [2014-06-06 pią 08:26] \\
  -1 - any child at all.
- State "CANCELLED"  from "TODO"       [2014-06-05 czw 15:31] \\
  Wat. 0 = any child, same group, -1 = any child.
#+end_example

Both list items should be followed by a paragraph consisting of just text.

3. Indented objects in the list item body
- Since we're dealing with the list items' body, please ensure that list item bodies also include drawers that are indented to the same level as the list item.

- Add support for blocks in the list item body, same as with drawers.

- Add support for fixed-width section in the list item body, same as with drawers.

*** DONE Cookie edge cases
:LOGBOOK:
- State "DONE"       from "TODO"       [2026-03-05 Thu 15:06]
:END:
1. Square brackets that don't constitute a priority =[#]=, a counter =[/]= or =[%]= should be parsed as plain text.

#+begin_example
,* Zombie apocalypse [sic] kit. [21/30]
- Test [value] [%]
#+end_example

Here the title should have a single counter and the initial bracket should be treated as text. Same for the list item.

Another example:
#+begin_example
- Input[0:INPUT_BUS_SIZE] is the value passed into the continuation,
- NextFunction[0:FUNCTION_BUS_SIZE] is the next function to call,
- NextValue[0:INPUT_BUS_SIZE] is the next value passed into the continuation,
#+end_example

Here all three list items should consist only of text.

*** DONE More drawer edge cases
I have in my Org archive the following drawer that doesn't parse:

#+begin_example
:LOGBOOK:  
:LOGBOOK:
- State "DONE"       from "TODO"       [2026-03-05 Thu 16:05]
:END:
- State "DONE"       from "TODO"       [2021-01-08 Fri 00:12]
    - State "CANCELLED"  from "TODO"       [2016-01-24 Sun 20:03] \\
      Not quite good enough.
    - State "DONE"       from "TODO"       [2015-01-11 Sun 14:29]
    - State "DONE"       from "TODO"       [2015-01-09 Fri 18:03]
    :END:      
#+end_example

**** COMMENT
Fixing this reverted some of the previous work on getting indented blocks to be a part of the list item body. Needs another attempt.

Can you please make drawers work with mis-aligned ends?
*** DONE Table edge cases
:LOGBOOK:
- State "DONE"       from "TODO"       [2026-03-05 Thu 16:46]
:END:
Tables separated by blank lines are failing to parse:

#+begin_example
| Foo | Bar |

| Baz | | Bar | Foo |
#+end_example

When there's text on the blank line the second table parses fine.

Fixing this broke the TBLFM parsing:

#+begin_example
|------------+---------+-------+-----------------+---------+------------------+--------------------+------------------|
|    *Total* |         |       |                 |         |              152 |                115 |               37 |
#+TBLFM: @2$5..@>>$5='(format-consultancy-hours "%h:%.2m" $1 $2 $3 $4)
#+end_example

The above line should parse fine with the table formula field attached to the table.

*** DONE Collons in text
:LOGBOOK:
- State "DONE"       from "TODO"       [2026-03-05 Thu 16:59]
:END:
Here's an edge case of collons in text paragraphs:

#+begin_example
This doesn't work. :shrug:
#+end_example

The =:shrug:= string should be treated as plain text and be a part of the whole line.

*** DONE More square bracket issues
:LOGBOOK:
- State "DONE"       from "TODO"       [2026-03-05 Thu 22:48]
:END:
Here are some more issues to tackle:

1. The ={= and =}= in links description seems to break the parser:
#+begin_example
[[file:~/org/TODO::*Refile%20stuff%20from%20%5B%5B~/org/refile.org%5D%5Brefile.org%5D%5D.][Refile stuff from {{~/org/refile.org}{refile.org}}.]]
#+end_example
The description should parse fine with the curly-braced value treated as text.

2. Indented links and timestamps break the parser, removing the indent makes them parse correctly.
#+begin_example
 [2011-11-27 nie 13:39]
 [[file:~/org/TODO::*Repeat%20to%20German%20test.][Repeat to German test.]]

 [2011-11-27 nie 16:00]
 [[file:~/org/notes.org::*Bisection][Bisection]]
#+end_example
All of these should parse despite the indent, both under a heading and under a list item.

The same indent issue is true for the angle-bracket timestamps and links.

There's still an issue with the curly bracket it seems:
#+begin_example
[[file:~/org/TODO::*Read%20%5B%5B/media/Transcend/books/compilers/Structure%20and%20Interpretation%20of%20Computer%20Programs.pdf%5D%5BStructure%20and%20Interpretation%20of%20Computer%20Programs%5D%5D][Read {{/media/Transcend/books/compilers/Structure and Interpretation of Computer Programs.pdf}{Structure and Interpretation of Computer Programs}}]]
#+end_example

There's an issue with square brackets and backslashes:
#+begin_example
[[file:~/Dokumenty/presentation/beamerthemeIdorobots.sty::\DeclareOptionBeamer{compress}{\beamer@compresstrue}]]
#+end_example

*** DONE Backslash
:LOGBOOK:
- State "DONE"       from "TODO"       [2026-03-05 Thu 22:40]
:END:

Backslash causes issues within bodies of some org nodes. Here's an example LaTeX body:

#+begin_example
\noindent
A matrix $A$ is well conditioned when $cond(A)$ is small. Otherwise
matrix $A$ is ill conditioned. The conditioning of a matrix $A$ is
given as:

\[ cond(A) = \textbar \textbar A \textbar \textbar \cdot \textbar
\textbar A^{-1} \textbar \textbar\]

\noindent
where $\textbar \textbar A \textbar \textbar$ is the norm of the matrix $A$:

- *first norm* (column norm) - given $n = dim(A)$, $i$ - row index,
  $j$ - column index:

\[ ||A||_1 = \max_{1 \leq j \leq n} \sum^n_{i=1} |a_{i,j}| \]

- *second norm* (spectral norm) - given $\lambda_i$ - ith eigenvalue of
  the matrix $A$, $\rho(M)$ - spectral radius of the matrix $M$:

\[ ||A||_2 = \sqrt{\rho(A^T A)} \]
\[ \rho(M) = max \{|\lambda_i| : det(M-\lambda_i I) = 0, i = 0, \dots,
n\} \]

\noindent
The spectral radius of matrix $M$ is the maximum absolute eigenvalue
$\lambda$ of this matrix.

- *$\infty$ norm* (row norm):

\[ ||A||_\infty = \max_{1 \leq i \leq n} \sum^n_{j=1} |a_{i,j}| \]

\noindent
Computing conditioning is not trivial. It becomes easier when matrix
$A$ is symetrical. Using second norm:

\[ cond_2(A) = ||A||_2 \cdot ||A^{-1}||_2 = \frac{\max_{1 \leq i \leq
n} |\lambda_i|}{\min_{1 \leq i \leq n}  |\lambda_i|} \]
#+end_example

I'd like these backslashes to be treated as text. There's no need to support LaTeX embeds. All of the above fragments preceeded by a backslash should parse as plain text.
Please simplify the example for the test corpus.

This line is still failing:
#+begin_example
\textbar A^{-1} \textbar \textbar\]
#+end_example

Backslash in the heading title is not parsed correctly:

#+begin_example
,* DONE sed \\* to something else in the notes.                    :COMP:note:
#+end_example

Here, the =\\*= string should be treated as text.

*** DONE Malformed counters
:LOGBOOK:
- State "DONE"       from "TODO"       [2026-03-05 Thu 22:56]
:END:
List counters with an invalid value should be treated as text:

#+begin_example
- [X] multiplying / adding random numbers
- [?] evaluating all kinds of formulae
#+end_example

Here the second line should not have a counter on it.

*** DONE Comment indents
:LOGBOOK:
- State "DONE"       from "TODO"       [2026-03-05 Thu 22:50]
:END:
I have some Lisp code as part of a body of one of the tasks:

#+begin_example
(module foo (printer alocator)
  # ...
)
#+end_example

The above example should be treated as text, with the exclusion od the =# ...= part, which is a comment with a leading indent.

*** DONE Free form link paths
:LOGBOOK:
- State "DONE"       from "TODO"       [2026-03-05 Thu 23:49]
:END:
Looks like the links in Org Mode can have an optional free-form path after a double collon =::=. Here's an example that currenty doesn't parse very well:

#+begin_example
[[file:~/code/asm/tests/asm/unions.d::if(isOP)%20{%20//%20TODO%20Pretty%20up%20or%20make%20a%20binary%20dump.][file:~/code/asm/tests/asm/unions.d::if(isOP) { // TODO Pretty up or make a binary dump.]]
#+end_example

I found another issue with URL encoded values:

#+begin_example
[[file:~/projects/code/mall-sim/docs/docs.tex::\begin{figure}%5Bh!%5D][file:~/projects/code/mall-sim/docs/docs.tex::\begin{figure}[h!]]]
#+end_example

There are more, more complicated examples:

#+begin_example
[[file:~/.emacs.d/site-lisp/gamify.el::(insert%20(format%20"node%20%5Bshape%3D%25s,%20width%3D%25.2f,%20color%3D\"#e0e0e0\",%20fontcolor%3D\"#d8d8d8\",%20fixedsize%3Dtrue%5D%3B"][file:~/.emacs.d/site-lisp/gamify.el::(insert (format "node [shape=%s, width=%.2f, color=\"#e0e0e0\", fontcolor=\"#d8d8d8\", fixedsize=true];"]]
#+end_example

Seems like the part that comes after =::= should really be arbitrary.

Now this one is failing:
#+begin_example
[[file:~/projects/code/mall-sim/src/sim/model/algo/Tactical.java::if(x%20%3D%3D%20i%20&&%20y%20%3D%3D%20j)%20continue%3B][file:~/projects/code/mall-sim/src/sim/model/algo/Tactical.java::if(x == i && y == j) continue;]]
#+end_example

Here's another link description issue:

#+begin_example
    :PROPERTIES:
    :gamify_exp: 6
    :capture_time: [2013-08-20 wto 12:21]
    :context:  [[file:/flood/src/flood_manager.erl::handle_call(Request,%20From,%20State)%20->%0A%20lager:warning("Unhandled%20Manager%20call%20change."),%0A%20{reply,%20ok,%20State}.%0A][file:/flood/src/flood_manager.erl::handle_call(Request, From, State) ->
    lager:warning("Unhandled Manager call change."),
    {reply, ok, State}.
    ]]
    :END:
#+end_example

This time an entire erlang term is included in the link, and that term is multiline. Please make multi-line descriptions possible.

The above fix broke timestamps in drawers:

#+begin_example
,** TODO Test
:PROPERTIES:
:LAST_REPEAT: [2025-06-28 sob 14:16]
:END:
#+end_example

The above example used to work, but to longer does.

There's still a problem, but with scanner sync when leaving a drawer and moving to another one:

#+begin_example
:PROPERTIES:
:context: [[file:~/tmp::*Default FILE to *.org in cwd][Default FILE to *.org in cwd]]
:END:
:LOGBOOK:
- State "DONE"       from "TODO"       [2026-02-21 Sat 17:52]
:END:
#+end_example

Here the timestapm on the logbook item fails to parse.

An analogous, but reversed situation fails now:

#+begin_example
,*** Test
    :LOGBOOK:
- State "DONE"       from "TODO"       [2013-08-28 śro 08:51]
    :END:
    :PROPERTIES:
    :context:  [[file:/flood/src/flood_manager.erl::handle_call(Request,%20From,%20State)%20->%0A%20lager:warning("Unhandled%20Manager%20call%20change."),%0A%20{reply,%20ok,%20State}.%0A][file:/flood/src/flood_manager.erl::handle_call(Request, From, State) ->
    lager:warning("Unhandled Manager call change."),
    {reply, ok, State}.
    ]]
    :END:
#+end_example

Another case that fails:
#+begin_example
[[file:/flood/src/floodfsm.erl::io:format("~w:%20~s\n",%20%5Bself(),%20Msg%5D).][file:/flood/src/floodfsm.erl::io:format("~w: ~s\n", [self(), Msg]).]]
#+end_example

It's still not quite right:
#+begin_example
,** Test
:LOGBOOK:
- State "DONE"       from "TODO"       [2013-05-20 pon 09:28]
:END:
:PROPERTIES:
:context: [[file:/test.erl::handle_call({kill_clients,%20Number},%20_,%20Clients)%20->][file:/brain-flood/src/flood_serv.erl::handle_call({kill_clients, Number}, _, Clients) ->]]
:END:
#+end_example

There's new failures:
#+begin_example
[[file:/brain-server/.config::{backend,%20cowboy}%5D}%5D][file:/brain-server/.config::{backend, cowboy}]}]]]
[[file:/brain-server/src/brain_cowboy.erl::Dispatch%20%3D%20cowboy_router:compile(%5B{'_',%20%5B{"/wspoll/:timeout",%20brain_cowboy_websocket_handler,%20%5B%5D},][file:/brain-server/src/brain_cowboy.erl::Dispatch = cowboy_router:compile([{'_', [{"/wspoll/:timeout", brain_cowboy_websocket_handler, []},]]
#+end_example

*** DONE More special keywords
:LOGBOOK:
- State "DONE"       from "TODO"       [2026-03-05 Thu 23:11]
:END:
Add support for additional keywords:

- =#+CONSTANTS:=

If it easy enough, make arbitrary special keywords valid.

*** DONE Drawers & links
There seems to be a lingering issue with scanner state reset when multiple consecutive headings have properties/logbook drawers:

#+begin_example
,*** Test
    :PROPERTIES:
    :context:  [[file:/flood/src/flood_fsm.erl::{Url,%20%5B{"connection","keep-alive"},][file:flood/src/flood_fsm.erl::{Url, [{"connection","keep-alive"},]]
    :END:
    Ibrowse with set_max_sessions seems to work, but requires host and port.

,*** Another test
    :LOGBOOK:
- State "DONE"       from "ACTIVE"     [2013-08-19 pon 15:00]
- State "ACTIVE"     from "TODO"       [2013-08-19 pon 12:58]
    :END:
#+end_example

All of the above should parse fine with =:context:= value ending on the same line. I do realise there's an extra =[= character in there that might be throwing off the parser.

Parsing fails when there is some text on the same line as the drawer start:

#+begin_example
,*** DONE Test
    :LOGBOOK:  1
- State "DONE"       from "ACTIVE"     [2013-08-19 pon 16:08]
- State "ACTIVE"     from "TODO"       [2013-08-19 pon 15:00]
    :END:
#+end_example

I'd like these cases to be handled even though this is technically wrong. The =1= should be a part of the drawer body along with the list that follows it. Note the misalignment of the list items relative to the drawer. That should also be handled.

Here's an edge case that fails parsing:
#+begin_example
 :PROPERTIES:
 :context:  [[file:/deps/websocket_client/src/websocket_client.erl::io:format("---->>>%20m:%20~s",%20%5BBuffer%5D),][file:/deps/websocket_client/src/websocket_client.erl::io:format("---->>> m: ~s", [Buffer]),]]
 :END:
#+end_example
Note the initial indent, when the indent is removed the error is different.

The =[= and =]= characters in the link description are still causing an issue:
#+begin_example
,*** DONE JSON.
    :PROPERTIES:
    :gamify_exp: 15
    :capture_time: [2013-08-23 pią 08:29]
    :context:  [[file:/examples/2.json::%5B"on_socketio",%20{"opcode"%20:%20"0",%20"do"%20:%20%5B][file:/examples/2.json::["on_socketio", {"opcode" : "0", "do" : []]
    :END:
#+end_example

I think we could introduce a heuristic - a link with a description should encompass anything until a =]]= is encountered followed by a newline character. Would that have solved this issue? Would there be any regressions introduced?

It's still breaking when there's a logbook drawer before the properties:
#+begin_example
,*** DONE More JSON
    :LOGBOOK:
:LOGBOOK:
- State "DONE"       from "TODO"       [2026-03-06 Fri 14:50]
:END:
- State "ACTIVE"     from "TODO"       [2013-08-23 pią 13:39]
    :END:
    :PROPERTIES:
    :context:  [[file:/flood/examples/2.json::%5B"on_socketio",%20{"opcode"%20:%20"0",%20"do"%20:%20%5B][file:/examples/2.json::["on_socketio", {"opcode" : "0", "do" : []]
    :END:
#+end_example

One more edge case found:
#+begin_example
    :PROPERTIES:
    :context:  [[file:/src/flood_fsm.erl::%5BSid,%20_Heartbeat,%20_Timeout,%20_Transports%5D%20%3D%20binary:split(Msg,%20<<":">>,%20%5Bglobal%5D),][file:/src/flood_fsm.erl::[Sid, _Heartbeat, _Timeout, _Transports] = binary:split(Msg, <<":">>, [global]),]]
    :END:
#+end_example

One more edge case popped up:
#+begin_example
:PROPERTIES:
:context: [[file:/brain-server/.config::{backend,%20cowboy}%5D}%5D][file:/brain-server/.config::{backend, cowboy}]}]]]
:END:
#+end_example

*** DONE More oddball issues
:LOGBOOK:
- State "DONE"       from "TODO"       [2026-03-06 Fri 15:21]
:END:
Some indentation problems were found.

1. Indented paragraph should be accepted:
#+begin_example
- disconnect (optionally abruptly).

    Flood FSM modifications:
- execute a script once connected.
#+end_example

I expect the indented line to count as a fresh paragraph.

Some random lines with code don't parse even though there doesn't seem to be any inline markup confusion htere:
#+begin_example
    %%jsonx:encode(comet_monitor:get_metrics(<<"foo.bar">>, [<<"foo.bar.baz">>, <<"foo.bar.faz">>, <<"foo.baz.bar">>, <<"foo.derp">>], [23, 5, nope, 13])).
#+end_example
The above should be treated as plaintext.

Invalid timestamps sometimes happen and should be parsed as text:
#+begin_example
,* DONE Timestamps
   DEADLINE: <2013-02-25 8:00>
   SCHEDULED: [2013-08-07 śro ++1da]
   CLOSED: .<2013-10-12 śro>
#+end_example
In this case, all of these lines following the heading should be recognized as a planning, but the timestamps should be plain text. We will add another pass eventually that will validate the parse tree for well-formedness and then we might roll back this error recovery to produce a parse error instead, but for now let's treat these as plaintext.

*** DONE Link description edge cases
:LOGBOOK:
- State "DONE"       from "TODO"       [2026-03-06 Fri 16:32]
:END:
Here are some more edge cases:
#+begin_example
:properties:
:context: [[file:~/main.cpp::std::cout%20<<%20i->first%20<<%20"%20-->%20"%20<<%20i->second%20<<%20"\n"%3B][file:~c/main.cpp::std::cout << i->first << " --> " << i->second << "\n";]]
:end:
#+end_example

#+begin_example
:properties:
:context: [[file:/echo_hook.erl::Ack%20->%20{reply,%20%5B#sio_message{type%20%3D%20ack,%20data%20%3D%20hive_socketio_parser:encode_id(Ack)}%5D,%20State}][file:/echo_hook.erl::Ack -> {reply, [#sio_message{type = ack, data = hive_socketio_parser:encode_id(Ack)}], State}]]
:end:
#+end_example

#+begin_example
:properties:
:context: [[file:/hive-server.conf.j2::command%5Bcheck_hive_uptime%5D%3D/usr/lib/nagios/plugins/check_http%20-I%20localhost%20-u%20"/socket.io/1/"%20-p%20{{%20hive_port%20}}%20-r%20-w%200.5%20-c%201.0][file:/hive-server.conf.j2::command[check_hive_uptime]=/usr/lib/nagios/plugins/check_http -I localhost -u "/socket.io/1/" -p {{ hive_port }} -r -w 0.5 -c 1.0]]
:end:
#+end_example

#+begin_example
:properties:
:context: [[file:/hive-server.conf.j2::command%5Bcheck_hive_connection%20%5D%3D/usr/lib/nagios/plugins/check_http%20-I%20localhost%20-u%20"/socket.io/1/"%20-r%20"Hive"%20-p%20{{%20hive_port|mandatory%20}}%20-k%20"Origin:%20{{%20market_vars.domain%20}}"%20-w%200.5%20-c%201.0][file:/hive-server.conf.j2::command[check_hive_connection ]=/usr/lib/nagios/plugins/check_http -I localhost -u "/socket.io/1/" -r "Hive" -p {{ hive_port|mandatory }} -k "Origin: {{ market_vars.domain }}" -w 0.5 -c 1.0]]
:end:
#+end_example

*** DONE Code markup regression
:LOGBOOK:
- State "DONE"       from "TODO"       [2026-03-06 Fri 19:18]
:END:
I found some regressions in how the code inline markup works with brackets:
#+begin_example
text ~dict[str, int]~.
  indent  ~dict[date, int]~
text ~TimeRange.update()~
(effectively a ~dict[str, str]~).
text ~analyze()~
text ~.[O:L]~
#+end_example

Another regresion when there are two dashes in text:
#+begin_example
,*** --watch & --serve switches
--watch for file watching and updating live on changes
--serve to start a server showing a visualization of the data
#+end_example

All the lines above should be text, even the heading title.

*** CANCELLED Other edge cases
:LOGBOOK:
- State "CANCELLED"  from "TODO"       [2026-03-08 Sun 13:18] \\
  Documented, will be addressed in the future.
:END:
- Nested lists are parsed as items on the parent list, but include an indent
- Malformed objects are usually treated as plaintext.

** DONE A script for checking example file coverage
:LOGBOOK:
- State "DONE"       from "TODO"       [2026-03-05 Thu 18:12]
:END:
Create a simple script that can be run to get a breakdown of which files from the examples directories are still failing to parse correctly.
The script should run the parser against each example file and list any errors that are encontered together with the content that causes the issue and a two lines of context on each side of the offending lines.

Example invocation:
#+begin_src bash
./check.py example/*.org
#+end_src

The output of the script should look like this:

#+begin_example
46/53 files passing

# examples/footnotes.org:

```
some text that is failing
to parse
with context on either side
```

```
another failure in this file
```

# examples/zeroth.org

```
...
```

...
#+end_example

** CANCELLED A script for minimizing failing parse examples
:PROPERTIES:
:ORDERED:  t
:END:
:LOGBOOK:
- State "CANCELLED"  from "TODO"       [2026-03-05 Thu 22:42] \\
  Didn't proove useful as it would minimize the error out and return unmatched drawers. Settled on just having the leaf errors listed.
:END:
Create a Python script called =minify.py= that will take a file as an input, run the parser on that file and if any errors are encountered, it will attempt to minimize the input file (without modifying it, use temporary/in-memory file instead) to be as small as possible but still reproduce the error.

The minification process should try to remove lines for as long as the error still persists.
Once the problematic fragment is removed and the error is no longer produced it should backtract one iteration and display the results.
The goal is to have the smallest possible fragment that triggers a parser issue.

If a file has multiple issues, just focus on the very first one on the list. Use the leaf error, not the wrapper over the whole subtree.

Let's adjust the script to isolate each initial error and attempt to find a smallest fragment within its context that reproduces it.

Let's try something different, make the script only find the leaf errors and display them together with configurable context. No minification needed. You can remove all the minification code.

By leaf errors I mean errors that are as close to a leaf as possible, not the top level errors, not necessarily errors at the very leaf of the expression - just the final level at which an error occurs that does not have an error embedded in them.

* DONE Semantic nodes implementation
:LOGBOOK:
- State "DONE"       from "TODO"       [2026-03-21 Sat 00:47]
:END:
** DONE Python scaffold
:LOGBOOK:
- State "DONE"       from "TODO"       [2026-03-07 Sat 13:41]
:END:
Create a scaffold of a Python project using Poetry as the build tool. Include =mypy= for typings, =ruff= for linting and formatting and =pytest= for tests. The project will be a library that wraps the tree-sitter based parser, so include =py-tree-sitter= in the dependencies as well. For development, add =pyright= LSP server.
Make sure that the setting for all of these tools are fairly strict, we want this project to be very well validated.

I'd like the layout of the library to be as follows:

#+begin_example
src - main Python source folder
|-- org_parser - main module
    |-- document - document-level objects
    |-- element - lesser & greater elements
    |-- text - rich text support
tests - the test suite
#+end_example

The source code of the library should expose the tree-sitter parser with thin Python bindings. Don't worry about ease-of-use of the parser just yet, we will be addressing the user experience next. It is enough to provide an =org_parser.document.load_raw()= function that reads an org file and returns th raw parse tree.

Create a test suite that will run the parser against some basic example files. We will be expanding the code soon, so there's no need to validate that it works too much.

Before starting, write the implementation plan to docs/plans/ as a markdown file.

** DONE Update the CI & AGENTS.md
:LOGBOOK:
- State "DONE"       from "TODO"       [2026-03-07 Sat 13:41]
:END:
Update GitHub Actions workflow to also test the python portion of the project.

Update AGENTS.md with instructions for the Python portion of the project.

** DONE Semantic nodes
:LOGBOOK:
- State "DONE"       from "TODO"       [2026-03-19 Thu 01:08]
:END:
*** DONE Env, Document, Heading
:LOGBOOK:
- State "DONE"       from "TODO"       [2026-03-08 Sun 13:39]
:END:
Add the top-level semantic classes to the =org_parser.document= package:

1. =Document=

Represents the full Org-Mode document structure, including the zeroth section and headings found within the document.

It should expose the following public properties:

- =filename: str= - the filename of the document file,
- =title: RichText | None= - the value of the TITLE keyword
- =author: RichText | None= - the value of the AUTHOR keyword
- =category: RichText | None= - the value of the CATEGORY keyword
- =body: list[Element]= - returns the zeroth section of the document, excluding any headings
- =children: list[Heading]= - returns all the top-level headings of this document


2. =Heading=

Represents a heading or a subheading object. It should expose the following public properties:

- =document: Document= - the document in which the heading is found
- =level: int= - the count of stars on the heading
- =todo: str | None= - the TODO state (if any) of the heading
- =priority: str | None= - the priority of the heading
- =title: RichText | None= - title text as rich text
- =counter: str | None= - completion counter for the heading
- =tags: list[str]= - a list of tags of the heading, text-order
- =body: list[Element]= - returns the body section of the heading, excluding any subheadings.
- =parent: Heading | Document= - heading or document that is a parent of this heading,
- =children: list[Heading]= - list of subheadings of this heading
- =siblings: list[Heading]= - list of siblings of this heading (not including this heading)


Don't worry about the definition of the =Element= class, let's create a stub for that in =or_parser.elements.*= module.
Don't worry about the definition of the =RichText= class, let's create a stub for that in =or_parser.text.*= module.

All of these abstractions should be constructable using either the parse tree or by manually providing raw values.
When using the parse tree a special care should be taken to preserve the original source representation of the different objects, i.e. the title text should be kept verbatim as in the Tree-Sitter parse tree. We will lax that requirement eventually by introducing mutability & representation function that builds the representation from the components.

Please extract the =Heading.counter= completion counter from the title. The =counter= property should store only the inner value of the brackets or =None= if the counter is not present.

For the =Document.body=, just include everything that isn't a heading or a special keyword. Headings will be available as the =children= property, while special keywords should be extracted to individual fields of the document. For now let's recognize just the =title=, =author=, =category=, =description= and =todo= as dedicated properties of the document with all other special keywords plased in a =keywords: dict[str, RichText]= dictionary.

Use a factory class method for the construction of the objects from the parse tree nodes. The constructors should require only the bare minimum of arguments. We will add mutable setters at a later point. Do keep the parse tree node in the constructed object for later use (we will base the string representation of the objects on that parse tree node at a later point).

The =parent= property should be mandatory when constructing an object while the =children= property can be an empty list by default that is later updated. The =siblings= property can be computed, it won't be used too much, so there's no need to cache the values.

Please add some rudimentary tests for the functionality, but keep in mind that we will be expanding it.

*** DONE load, loads and dump, dumps
:LOGBOOK:
- State "DONE"       from "TODO"       [2026-03-08 Sun 14:23]
:END:
Add the folowing functions to the Python wrapper library:

- =load(filename: str): Document= - loads an Org-Mode file from file,
- ~loads(input: str, filename: str | None): Document~ - loads an Org-Mode data from a string with an optional ~filename~ assigned to the resulting document,
- =dumps(document: Document): str= - returns a string representing the contents of the document in Org-Mode format,
- ~dump(document: Document, filename: str | None)~ - saves the Org-Mode file in the file named by =document.filename=,

Add bsic tests for this functionality using the basic examples as test fixtures.

*** DONE Source bytes only in document
:LOGBOOK:
- State "DONE"       from "TODO"       [2026-03-08 Sun 14:35]
:END:
Do not cache the entire source file bytes in each heading. It suffices that the =Document= class caches the raw bytes and exposes them via the =source= property. Headings can use their =document= property to fetch the original source.
The constructor for headings can take the document as a parameter.

*** DONE Mutability
:LOGBOOK:
- State "DONE"       from "TODO"       [2026-03-08 Sun 14:56]
:END:
Implement setters for all the properties on all the objects so far.

Whenever, an object is mutated, it should be marked as dirty and it's representation should be built from scratch instead of re-using the raw parse tree.
You can assume for now that all objects will have a =__str__= property that can be used to turn the object to string. When an object is mutated, that property will instead build the representation from scratch.

We will implement the representation function next, so for the time being focus on the dirty flag and the setter logic.
Add tests for the setters, but omit the representation tests, we will address that next.

*** DONE mark_dirty()
:LOGBOOK:
- State "DONE"       from "TODO"       [2026-03-08 Sun 14:58]
:END:
Add a public method for marking the nodes as dirty programatically.

*** DONE __str__
:LOGBOOK:
- State "DONE"       from "TODO"       [2026-03-08 Sun 15:10]
:END:
Implement the string representation function =__str__= for all objects. By default, when the object is not marked "dirty", it should re-use the raw input and the Tree-Sitter parse tree to produce a verbatim (as-in-source) representation. When an object is marked as dirty, the representation should be reconstructed from scratch.

The representation for the =Heading= should only include the heading line and the body, but omit any subheadings.

The representation for the =Document= should only include the special keywords and the other elements of the zeroth section.

The goal is to preserve the original whitespace until a mutation is made, so that loading an object from the parse tree and then showing its representation will result in the same representation as in the parse tree source file.

Don't worry about the =Element= and =RichText= stubs just yet, we will address that later.

*** DONE RichText
:LOGBOOK:
- State "DONE"       from "TODO"       [2026-03-08 Sun 15:31]
:END:
Expand the abstraction of =RichText= in =org_parser.text.*= module to support the inline markup, links, footnotes, etc.
It should support all the parse tree nodes that can appear under a heading title or within a paragraph.

Please create abstarctions for all of these entities that can be a part of the =RichText=. =RichText= itself can be constucted by passing a list of these abstractions or with a class method taking a parse tree node.
=RichText= should have a dirty flag and by default show the verbatim document source representation when stringified. =RichText= can be mutated by prepending, appending or inserting additional data in the middle. That should mark it as dirty and cause the string representation to be computed.

All of the inline markup abstractions should cache the exact values they represent. No need to store the parse tree nodes or the document in them - we keept that at the =RichText= granularity.

All of these abstractions should be publicly available via the =org_parser.text.*= module so that new =RichText= objects can be created programatically and attached to headings/document.

Update the =Heading.counter= property to return a =CompletionCounter= instead of plain string.

*** DONE Heading planing
:LOGBOOK:
- State "DONE"       from "TODO"       [2026-03-08 Sun 16:15]
:END:
Expand the =Timestamp= class to store all the individual values of a timestamp, including the date, time, optionally a duration, active/inactive state etc.
Ideally, =Timestamp= should be moved to a dedicated module =org_parser.time= as there will be more related abstractions there.

Timestamps should have a =start= and =end= properties, with =end= being optional (only available if the timestamp has a duration). Timestamp should be convertible to a Python datetime (use the =start= property). There is no need to make the timestamp fields mutable.

No need to reimplement the timestamp parsing in Python - use the tree sitter node properties directly.

Add the planning fields to the =Heading= class:
- =scheduled: Timestamp | None=
- =closed: Timestamp | None=
- =deadline: Timestamp | None=

These fields should be mutable. Update the representation for =Heading= to take them into account when the heading is marked as dirty.

*** DONE Element
:LOGBOOK:
- State "DONE"       from "TODO"       [2026-03-08 Sun 16:36]
:END:
Expand the abstract =Element= class with an =parent= property. The parent can be either a =Document=, a =Heading= or another =Element=.

Whenever an =Element= is mutated, the parent element must be marked as dirty in addition to marking the current element as dirty. The same is true for the =Heading=, a mutation at a nested level should mark all intermediate levels as dirty, all the way to the root element - the =Document=, unless an element along the chain is already marked as dirty. This way a mutation at a lower level will be reflected when stringifying the whole =Document=.

Both =RichText= and =Timestamp= can be made into full-fledged elements if that makes sense.

**** COMMENT
Codex decided that making rich text an element doesn't make sense.

*** DONE Keywords
:LOGBOOK:
- State "DONE"       from "TODO"       [2026-03-08 Sun 16:55]
:END:
Add a dedicated =Element= subclass for the special keywords called =Keyword=.
The class should represent a special keyword. The keyword value should be mutable.

Update the =Document= to extract and store these new =Keyword= objects instead of =RichText= directly.

All keywords should be available in the =Document.keywords= map, including the ones with specialized properties.

*** DONE Paragraph
:LOGBOOK:
- State "DONE"       from "TODO"       [2026-03-08 Sun 17:01]
:END:
Add a dedicated =Element= subclass for the paragraph element - =Paragraph=. The class should store the values that are found in =paragraph= node of the parse tree. The paragraph body should be mutable.

Add support for this new element type when processing document/heading bodies that can contain paragraphs.

*** DONE Table
:LOGBOOK:
- State "DONE"       from "TODO"       [2026-03-08 Sun 17:12]
:END:
Add a dedicated =Element= subclass for the table element - =Table=. The class should represent an Org table or a Table.el table, with dedicated abstractions for table rows and table cells. Table cells and rows should be mutable, marking the whole table as dirty. Table cell values can remain =RichText= for now, but the parent element of these rich text elements should be the table.

Tables should also support the table formula lines.

When the element is marked dirty, the table should be formatted according to the Org Table rules, not Table.el. Make sure to align the table columns based on the size of the cell values. The resulting table should be "nicely" formatted and aligned.

Add support for this new element type when processing document/heading bodies that can contain tables.

*** DONE Clock
:LOGBOOK:
- State "DONE"       from "TODO"       [2026-03-08 Sun 17:22]
:END:
Add a dedicated =Element= subclass for the clock log line. This element should support the various forms a =CLOCK:= line can assume - including an active and inactive timestamp, a timerange and the duration.
The element should be placed in the =org_parser.time.*= module and re-use the =Timestamp= class for the actual time component.
Clocks should be mutable by setting the entire timestamp. The duration value should be recomputed after the timestamp is set with a duration (start and end populated).

Add support for this new element type when processing document/heading bodies that can contain clock lines.

*** DONE Properties, Logbook, Drawer
:LOGBOOK:
- State "DONE"       from "TODO"       [2026-03-08 Sun 17:59]
:END:
Add a dedicated =Element= subclass for the drawer elements: =Properties=, =Logbook= and =Drawer=. These classes should represent the Org Mode drawers, including the key-value pairs stored within them (for properties) as well as the other types of objects that can be a part of a drawer (other drawer types).

For =Properties= do extract the vlues from tree-sitter's =property_drawer=, specifically the key-value pairs for =node_property=.
I would like the =Poperties= element to be mutable and dictionary-like, i.e. it should be possible to set the values of properties via ~properties["name"] = RichText("test")~ for instance. The values of the properties should be "owned" by the drawer element, so that mutation of any of those field values is immediately reflected in the output of the drawer stringification.

For =Logbook= drawers, extract the list of logbook repeats and clock entries. For now, let's leave them as separate elements that are owned by the drawer. We will address the usability of the logbook drawer at some point.

For the remaining drawer types, have a dedicated =Drawer= element that represents the drawer. It should have a name and a body. The body is owned by the drawer.

All drawers should be mutable and marked dirty when mutated.

*** DONE Heading/Document drawers
:LOGBOOK:
- State "DONE"       from "TODO"       [2026-03-08 Sun 22:02]
:END:
Update the =Document= class with fields dedicated to properties and the logbook. These fields should be populated based on the properties & logbook drawers found on the parse tree. If there are multiple such drawers attached to a document's zeroth section merge their values into a single element dedicated to the given drawer type.

Update =Heading= class with fields dedicated to properties and the logbook. These fields should be populated based on the properties & logbook drawers found on the parse tree. If there are multiple such drawers attached to the heading merge their values into a single element dedicated to the given drawer type.

These extracted drawers should still be mutable and reflect their changes in the stringification output. For the dirty output, the properties drawer should come first, followed by the logbook drawer.

Other drawers can stay in the bodies of their respective owning elements. No need to handle them specially.

*** DONE Block
:LOGBOOK:
- State "DONE"       from "TODO"       [2026-03-08 Sun 22:49]
:END:
Add a dedicated =Element= subclass for the various blocks: =ExampleBlock=, =VerseBlock=, =SourceBlock=, =DynamicBlock=, =FixedWidthBlock=, etc. The classes should represent the different blocks of the Tree Sitter parse tree. Blocks should be mutable, with the contents of the blocks specifically being mutable. Mutation should be handled the same way as of the other elements.

Add support for these new elements when processing document/heading bodies that can contain blocks.

*** DONE List
:LOGBOOK:
- State "DONE"       from "TODO"       [2026-03-08 Sun 23:15]
:END:
Add a dedicated =Element= subclass for the =List='s. The class should represent a list comprised of =ListItem='s. List items should support all the features of the list items, such as the completion counters, checkboxes, item tags, rich text first line, the continuation lines, etc. List items should be mutable, and so is the list itself - it should be possible to add extra line items to the list.

Add support for this new element when processing document/heading bodies that can contain lists. Do also add support for these in drawer bodies.

*** DONE Repeated tasks
:LOGBOOK:
- State "DONE"       from "TODO"       [2026-03-09 Mon 00:20]
:END:
Add a dedicated =Element= subclass for repeated tasks. A =Repeat= is a list item stored in the =LogbookDrawer= as one of the following forms:

#+begin_example
- State "<after_state>" from "<before_state>" <timestamp>

- State "<after_state>" from "<before_state>" <timestamp> \\
  <state_change_note>
#+end_example

That is, it is a line item with a specific text content followed by a timestamp and an optional linebreak and optional continuation line containing a short note. The items and continuation lines can be arbitrarily indented. Here are some actual examples:

#+begin_example
- State "DONE"       from "TODO"       [2026-03-08 Sun 17:59]
- State "CANCELLED"  from "TODO"       [2026-03-08 Sun 13:18] \\
  No need for that with the semantic nodes.
#+end_example

The line item content should be parsed to extract the =after= and =before= states as well as the =timestamp= and the optional =note=. The =Repeat= class should cache these values internally and allow their mutation (causing the containing list to become dirty).

The =Repeat= class should specifically be used in the logbook drawer. No other lists should be parsed for the repeated task format.

Add a new field to the =Heading= class called =repeated_tasks: list[Repeat]=. It should contain all the repeats from the logbook drawers of that heading. The filed should be mutable and allow adding extra repeats (effectively mutates the list found in the logbook).

*** DONE Error parse fallback
:LOGBOOK:
- State "DONE"       from "TODO"       [2026-03-18 Wed 18:42]
:END:
Add a semantic fallback whenever a parse error node is encountered - the invalid parse node should be treated as a =RichText= element.

Add special handling of these error situations in extraction code, so that the resulting semantic node tree uses those =RichText= elements instead of throwing any errors or ignoring the error condition. If a textual value absolutely cannot be used, then prefer to treat the "enclosing" object as text instead of throwing. The goal is to recover from the error condition and make the remained of the parse tree usable as is.

At the same time, whenever a parse tree error is encountered, please store that error parse node in the root =Document= class under the =errors= list. This can be useful for determining if there are any errors present in the file (linting, etc).

The =report_error= callback should instead be a method of the =Document= class. All elements & functions that receive the =Document= can just use that method to report any encountered parse errors. It makes sense to pass the =Document= that owns each element to those elements instead of the raw =source= and the extra =report_error=. I expect elements to receive the parse tree node that they represent, the =Document= for the environment in which the parse tree node is valid, and the other arguments an element might need.

=Document= and =Heading= should also recognize the parse errors that can occur in the zeroth section (for =Document=) or the various constituents of the =Heading= - a parse error can be encountered at any parse node and these should be handled by attaching them to the owning document and treating the error as text for the semantic node creation.

Here's a string input that should produce an error in the document but doesn't: ~"* test\n:properties:\n"~.

For =ParseError= please only expose the start & end points and the text, no need to expose the parse tree node via instance attributes. A method called =ParseError.raw= should be added to return the raw parse tree node.

*** CANCELLED raw()
:LOGBOOK:
- State "CANCELLED"  from "TODO"       [2026-03-11 Wed 21:48] \\
  Decided not to do that.
:END:
Add a method called =raw= for each object that returns the raw Tree-Sitter parse tree nodes that were used to construct those objects.

*** DONE More convenient element __repr__
:LOGBOOK:
- State "DONE"       from "TODO"       [2026-03-11 Wed 22:21]
:END:
Please add =__repr__= methods to all element classes that show the contents in a convenient way - instead of showing the verbatim source text it should be showing the embedded elements. Only show the populated fields, no =None='s.

Also update the representation of =Document= and =Heading=.

The goal is to be able to quickly see the semantic tree structure at a glance.

** DONE Recover semantic structure
:LOGBOOK:
- State "DONE"       from "TODO"       [2026-03-11 Wed 21:48]
:END:
Attempt to recover the nesting structure using the indent blocks and specifically the =indent= field of the =block= node.

Each block should be considered separately, when two consecutive blocks are of the same indent level, they still should be considered separately.

Within a block's body:

- Multiple consecutive paragraphs should be combined into a single paragraph object preserving the source contents & whitespace. Example:

  #+begin_example
  (block
    (paragraph
      (plain_text))
    (paragraph
       (plain_text)))
  ;; Becomes:
  (paragraph
    (plain_text)
    (plain_text))
  #+end_example

- Multiple consecutive list items should be combined into a single =List= object preserving the original source whitespace. Each parse tree item becomes an item on the list object. Here's an example:

  #+begin_example
  (block
    (list_item)
    (list_item)
    (list_item))
  ;; Becomes:
  (list
    (list_item)
    (list_item)
    (list_item))
  #+end_example

- When a nested block is following a list item, it should be considered for structure recovery recursively and then its results should be added as a body of the preceeding list item. Example:

  #+begin_example
  (block
    (list_item)
    (block
      (list_item)
      (list_item))
    (list_item))
  ;; Becomes:
  (list
    (list_item
      (list
        (list_item)
        (list_item)))
    (list_item))
  #+end_example

- Blank lines and other non-list-item elements should break up the lists/paragraphs in a block. A single block can contain multiple recoverable lists/paragraphs. Here's an example:

  #+begin_example
  (block
    (list_item)
    (paragraph)
    (list_item))
  ;; Becomes:
  (list
    (list_item))
  (paragraph)
  (list
    (list_item))
  #+end_example

The resulting objects should have the proper as-in-source string representation, including the nodes that were "semantically attached" to them.

After the recovery process, the semantic tree should not have any indent blocks present.

When stringifying a dirty list, please take into account the indentation - each level should use 2 spaces for indentation (but please keep that somewhat configurable).

** DONE Indent recovery
:LOGBOOK:
- State "DONE"       from "TODO"       [2026-03-11 Wed 22:35]
:END:
When stringifying a dirty list, please take into account the indentation - each level should use 2 spaces for indentation (but please keep that somewhat configurable). This pertains to all elements of the body of a dirty list - marking a list dirty should mark all of its items as dirty.

** DONE reformat method for re-formatting the text
:LOGBOOK:
- State "DONE"       from "TODO"       [2026-03-11 Wed 22:51]
:END:
Add a =reformat= method to =Document=, =Heading= and =Element= subclasses that will cause all subnodes of those nodes to be marked as dirty (i.e. call =reformat= on all descendant nodes and then mark self as dirty).

The goal is to be able to induce the scratch-built representation to be used instead of the source-verbatim one. This method will be used to selectively reformat a portion of an Org file.
** DONE Ensure that list recovery does result in the body of the list being shown when not dirty.
:PROPERTIES:
:capture_time: [2026-03-20 Fri 20:21]
:context: [[file:~/projects/software/org-mode-parser/src/org_parser/element/_list.py]]
:END:
:LOGBOOK:
- State "DONE"       from "TODO"       [2026-03-20 Fri 22:10]
:END:
** DONE Code review comments
:LOGBOOK:
- State "DONE"       from "TODO"       [2026-03-20 Fri 22:10]
:END:
*** DONE Element source & node type
:LOGBOOK:
- State "DONE"       from "TODO"       [2026-03-18 Wed 21:18]
:END:
Please refactor the =Element= class and its subclasses to not store the =source_text= directly and instead have =document= stored internally for the source needs. The =Element= class should also omit the =node_type= as it isn't needed. You can also omit its default =__str__= implementation, as that sholud never be called - the =Element= class is a subclass for the actual elements and should not be instantiated directly. You can also remove the =Element.from_node= method as elements should not be instantiated this way.

Do not define read only properties for `node_type` or `source_text`. Node type should be removed altogether as it serves no purpose. All places that use the source text should extract the underlying `document` and use its `source` attribute.

*** DONE Common __str__ helper
:LOGBOOK:
- State "DONE"       from "TODO"       [2026-03-18 Wed 22:27]
:END:
There's some duplication when getting the node source:

#+begin_example
+            return self._document.source[
+                self._node.start_byte : self._node.end_byte
+            ].decode()
#+end_example

Or...
#+begin_example
+            node = element._node
+            doc = element._document
+            raw = (
+                doc.source[node.start_byte : node.end_byte].decode()
+                if node is not None and doc is not None
+                else ""
+            )
+            line = raw.rstrip("\n")
#+end_example

Some modules define helperfs for this purpose:

#+begin_example
+def _node_text(node: tree_sitter.Node, source: bytes) -> str:
+    """Return source text covered by one node."""
+    return source[node.start_byte : node.end_byte].decode()
#+end_example

Or...

#+begin_example
+def _safe_node_text(node: tree_sitter.Node | None, source: bytes) -> str:
+    """Return node text or an empty string for missing nodes."""
+    if node is None:
+        return ""
+    return _node_text(node, source)
+
+
+def _node_text(node: tree_sitter.Node, source: bytes) -> str:
+    """Return source text covered by one node."""
+    return source[node.start_byte : node.end_byte].decode()
#+end_example

I'd like to introduce a generic helper in a common module that will address this accross the board - it should take the
=Document= and a tree sitter =Node= and return the source. It should handle the case wen either is not available.

*** DONE Reuse =recover_lists=
:LOGBOOK:
- State "DONE"       from "TODO"       [2026-03-18 Wed 22:48]
:END:
A lot of the modules define local wrappers over =recover_lists=, usually called =coalesce_list_items= or similar:

In file src/org_parser/element/_drawer.py around line 439, near the ~new file   src/org_parser/element/_drawer.py~

#+begin_example
+def _coalesce_list_items(
+    elements: list[Element],
+    *,
+    parent: Document | Heading | Element | None,
+) -> list[Element]:
+    """Recover semantic lists from flat drawer body elements."""
+    return recover_lists(elements, parent=parent)
#+end_example

In file src/org_parser/document/_body.py around line 207, near the ~new file   src/org_parser/document/_body.py~

#+begin_example
+def coalesce_list_items(
+    elements: list[Element],
+    *,
+    parent: Heading | Document,
+) -> list[Element]:
+    """Recover semantic lists from flat body elements.
+
+    Args:
+        elements: Body elements that may include raw ``list_item`` stubs.
+        parent: Owner heading or document.
+
+    Returns:
+        A new list where adjacent list items are grouped into
+        :class:`~org_parser.element._list.List` objects.
+    """
+    return recover_lists(elements, parent=parent)
#+end_example

In file src/org_parser/element/_block.py around line 800, near the ~new file   src/org_parser/element/_block.py~

#+begin_example
+def _coalesce_list_items(elements: list[Element]) -> list[Element]:
+    """Recover semantic lists from flat nested block body elements."""
+    from org_parser.element._list_recovery import recover_lists
+
+    return recover_lists(elements, parent=None)
#+end_example

There's likely more. Please replace all of these with straight calls to =recover_lists=.

*** DONE src/org_parser/__init__.py
:PROPERTIES:
:context: 
:END:
:LOGBOOK:
- State "DONE"       from "TODO"       [2026-03-19 Thu 19:39]
:END:

In file src/org_parser/__init__.py around line 57, near the ~new file   src/org_parser/__init__.py~
#+begin_example
+    return document.source.decode()
#+end_example

This only works when the source is available, it should use the representation function and iterate over the nodes. or the representation should include the children automatically.
*** CANCELLED src/org_parser/document/_heading.py
:PROPERTIES:
:context: 
:END:
:LOGBOOK:
- State "CANCELLED"  from "TODO"       [2026-03-19 Thu 18:06] \\
  Decided against it for easier iteration at a later point.
:END:

In file src/org_parser/document/_heading.py around line 476, near the ~new file   src/org_parser/document/_heading.py~

#+begin_example
+    # -- dunder protocols ----------------------------------------------------
+
+    def __str__(self) -> str:
+        """Return a textual representation of this heading and its body.
+
+        When the heading is clean and still backed by a parse tree, this
+        returns the exact source slice that spans the heading line and body
+        section only, excluding any sub-headings. Once dirty, this is rebuilt
+        from semantic fields.
+        """
+        if not self._dirty and self._node is not None:
+            end_byte = self._node.end_byte
+            first_subheading = _find_first_subheading(self._node)
+            if first_subheading is not None:
+                end_byte = first_subheading.start_byte
+            return self.document.source[self._node.start_byte : end_byte].decode()
+
+        return _render_heading_dirty(self)

#+end_example

This should include the children. Also, consider making =Heading= an =Element= so that we can just iterate over the body.
*** DONE src/org_parser/document/_document.py
:PROPERTIES:
:context: 
:END:
:LOGBOOK:
- State "DONE"       from "TODO"       [2026-03-19 Thu 22:39]
:END:

In file src/org_parser/document/_document.py around line 108, near the ~new file   src/org_parser/document/_document.py~

#+begin_example
+        self._source: bytes = b""
#+end_example

Ideally, it would be =None= when not available.
*** DONE src/org_parser/document/_body.py
:PROPERTIES:
:context: 
:END:
:LOGBOOK:
- State "DONE"       from "TODO"       [2026-03-18 Wed 21:18]
:END:

In file src/org_parser/document/_body.py around line 203, near the ~new file   src/org_parser/document/_body.py~
#+begin_example
+        source_text=source[node.start_byte : node.end_byte].decode(),

#+end_example

Should be the document instead, if source is missing it'll fail.
*** CANCELLED src/org_parser/document/_body.py
:PROPERTIES:
:context: 
:END:
:LOGBOOK:
- State "CANCELLED"  from "TODO"       [2026-03-18 Wed 21:33] \\
  There are more concerning things in the code.
:END:

In file src/org_parser/document/_body.py around line 35, near the ~new file   src/org_parser/document/_body.py~

#+begin_example
+if TYPE_CHECKING:

#+end_example

I don't like this pattern, please don't use it.
*** DONE src/org_parser/document/_body.py
:PROPERTIES:
:context: 
:END:
:LOGBOOK:
- State "DONE"       from "TODO"       [2026-03-18 Wed 22:34]
:END:

In file src/org_parser/document/_body.py around line 207, near the ~new file   src/org_parser/document/_body.py~

#+begin_example
+def coalesce_list_items(
+    elements: list[Element],
+    *,
+    parent: Heading | Document,
+) -> list[Element]:
+    """Recover semantic lists from flat body elements.
+
+    Args:
+        elements: Body elements that may include raw ``list_item`` stubs.
+        parent: Owner heading or document.
+
+    Returns:
+        A new list where adjacent list items are grouped into
+        :class:`~org_parser.element._list.List` objects.
+    """
+    return recover_lists(elements, parent=parent)

#+end_example

This looks like a wrapper that isn't needed. It literally just forwards the arguments to =recover_lists=.
*** DONE src/org_parser/document/_document.py
:PROPERTIES:
:context: 
:END:
:LOGBOOK:
- State "DONE"       from "TODO"       [2026-03-19 Thu 00:06]
:END:

In file src/org_parser/document/_document.py around line 209, near the ~new file   src/org_parser/document/_document.py~

#+begin_example
+    @title.setter
+    def title(self, value: Keyword | None) -> None:
+        """Set the ``#+TITLE:`` value and mark the document as dirty."""
+        self._title = value
+        if value is None:
+            self._keywords.pop(_TITLE, None)
+        else:
+            self._keywords[_TITLE] = value
+        self._adopt_keyword(self._title)
+        self._mark_dirty()
#+end_example

All of these keyword setters are duplicated with very few changes between them. They could be extracted into a helper method.
Please check the other classes for similar low-hanging fruit refactorings.

You can also define =adopt_elements= that accepts a list and use that instead of the various versions of =adopt_*= that extract parts of the object.

*** DONE src/org_parser/document/_document.py
:PROPERTIES:
:context: 
:END:
:LOGBOOK:
- State "DONE"       from "TODO"       [2026-03-19 Thu 00:08]
:END:

In file src/org_parser/document/_document.py around line 114, near the ~new file   src/org_parser/document/_document.py~
#+begin_example
+        self._adopt_keyword(self._title)
+        self._adopt_keyword(self._author)
+        self._adopt_keyword(self._category)
+        self._adopt_keyword(self._description)
+        self._adopt_keyword(self._todo)
+
#+end_example

It's duplicated with the following =_adopt_keywords= call.
*** DONE src/org_parser/document/_document.py
:PROPERTIES:
:context: 
:END:
:LOGBOOK:
- State "DONE"       from "TODO"       [2026-03-19 Thu 01:08]
:END:

In file src/org_parser/document/_document.py around line 412, near the ~new file   src/org_parser/document/_document.py~

#+begin_example
+        reformat_value(self._title)
+        reformat_value(self._author)
+        reformat_value(self._category)
+        reformat_value(self._description)
+        reformat_value(self._todo)
#+end_example

It's duplicated with the reformat call on =keywords=.
*** DONE src/org_parser/element/_element.py
:PROPERTIES:
:context: 
:END:
:LOGBOOK:
- State "DONE"       from "TODO"       [2026-03-19 Thu 01:08]
:END:

In file src/org_parser/element/_element.py around line 35, near the ~new file   src/org_parser/element/_element.py~

#+begin_example
+def reformat_value(value: object) -> None:
+    """Recursively mark semantic nodes dirty for scratch-built rendering."""
+    if value is None:
+        return
+
+    if isinstance(value, dict):
+        mapping = cast(dict[object, object], value)
+        for nested in mapping.values():
+            reformat_value(nested)
+        return
+
+    if isinstance(value, list | tuple | set):
+        sequence = cast(list[object] | tuple[object, ...] | set[object], value)
+        for nested in sequence:
+            reformat_value(nested)
+        return
+
+    reformat = getattr(value, "reformat", None)
+    if callable(reformat):
+        reformat()
+        return
+
+    mark_dirty = getattr(value, "mark_dirty", None)
+    if callable(mark_dirty):
+        mark_dirty()
#+end_example

I don't like this approach very much, I'd like it to resemble the =mark_dirty= or =adopt= patterns more closely - each =reformat= method should mark self as dirty and call =reformat= on any sub elements.

*** DONE src/org_parser/document/_document.py
:PROPERTIES:
:context: 
:END:
:LOGBOOK:
- State "DONE"       from "TODO"       [2026-03-19 Thu 00:07]
:END:

In file src/org_parser/document/_document.py around line 424, near the ~new file   src/org_parser/document/_document.py~

#+begin_example
    def _adopt_keyword(self, value: Keyword | None) -> None:
+        """Assign this document as parent for one keyword."""
+        if value is None:
+            return
+        value.set_parent(self, mark_dirty=False)
+
#+end_example

All of these are quite duplicated, instead these could be more generic functions accepting elements or collections of elemenths.
*** DONE src/org_parser/document/_document.py
:PROPERTIES:
:context: 
:END:
:LOGBOOK:
- State "DONE"       from "TODO"       [2026-03-19 Thu 00:08]
:END:


In file src/org_parser/document/_document.py around line 166, near the ~new file   src/org_parser/document/_document.py~

#+begin_example
+        doc._adopt_keyword(doc._title)
+        doc._adopt_keyword(doc._author)
+        doc._adopt_keyword(doc._category)
+        doc._adopt_keyword(doc._description)
+        doc._adopt_keyword(doc._todo)
+        doc._adopt_keywords(doc._keywords)

#+end_example

These are redundant as well.
*** DONE src/org_parser/document/_document.py
:PROPERTIES:
:context: 
:END:
:LOGBOOK:
- State "DONE"       from "TODO"       [2026-03-19 Thu 19:53]
:END:

In file src/org_parser/document/_document.py around line 580, near the ~new file   src/org_parser/document/_document.py~

#+begin_example
                else:
+                    body.append(
+                        extract_body_element(sc, parent=parent, document=parent)
+                    )
#+end_example

Needs to be checked for potential =ERROR= leak.
*** DONE src/org_parser/document/_heading.py
:PROPERTIES:
:context: 
:END:
:LOGBOOK:
- State "DONE"       from "TODO"       [2026-03-19 Thu 20:00]
:END:

In file src/org_parser/document/_heading.py around line 601, near the ~new file   src/org_parser/document/_heading.py~

#+begin_example
+    properties_node = node.child_by_field_name("properties")
#+end_example

Check if this can be multiple instances.
*** CANCELLED src/org_parser/element/_indent_block.py
:PROPERTIES:
:context: 
:END:
:LOGBOOK:
- State "CANCELLED"  from "TODO"       [2026-03-19 Thu 13:54] \\
  We do need it.
:END:

In file src/org_parser/element/_indent_block.py around line 19, near the ~new file   src/org_parser/element/_indent_block.py~

#+begin_example
+class IndentBlock(Element):

#+end_example

Not sure that we need an abstraction for this, as all of these blocks should be removed from the semantic tree once list recovery runs.

*** DONE src/org_parser/document/_heading.py
:PROPERTIES:
:context: 
:END:
:LOGBOOK:
- State "DONE"       from "TODO"       [2026-03-19 Thu 00:13]
:END:

In file src/org_parser/document/_heading.py around line 177, near the ~new file   src/org_parser/document/_heading.py~

#+begin_example
+        # Re-home body elements to this heading instance (not its parent).
+        heading._adopt_properties(heading._properties)
+        heading._adopt_logbook(heading._logbook)
+        heading._adopt_body_elements(heading._body)
#+end_example

I wonder if this could be done by passing the heading as a =parent= to =_extract_body=.
*** DONE src/org_parser/document/_heading.py
:PROPERTIES:
:context: 
:END:
:LOGBOOK:
- State "DONE"       from "TODO"       [2026-03-18 Wed 23:17]
:END:

In file src/org_parser/document/_heading.py around line 207, near the ~new file   src/org_parser/document/_heading.py~

#+begin_example
+        if not self._parent.dirty:
+            self._parent.mark_dirty()
#+end_example

I'd rather the =mark_dirty= method checked the dirty flag instead of doing it in call sites. Please update all such invocations to instead rely on =mark_dirty= to check the flag and only set it if unset. Make sure that all implementations of =mark_dirty= short-circuit to avoid loops.

*** DONE src/org_parser/document/_heading.py
:PROPERTIES:
:context: 
:END:
:LOGBOOK:
- State "DONE"       from "TODO"       [2026-03-18 Wed 23:17]
:END:

In file src/org_parser/document/_heading.py around line 209, near the ~new file   src/org_parser/document/_heading.py~

#+begin_example
+        if not value.dirty:
+            value.mark_dirty()

#+end_example

This seems weird. The dirty chain marking should do that anyway so no need to do this explicitly.
*** DONE src/org_parser/document/_heading.py
:PROPERTIES:
:context: 
:END:
:LOGBOOK:
- State "DONE"       from "TODO"       [2026-03-19 Thu 00:40]
:END:

In file src/org_parser/document/_heading.py around line 350, near the ~new file   src/org_parser/document/_heading.py~

#+begin_example
+        """Repeated task entries extracted from this heading's logbook."""
+        if self._logbook is None:
+            self._ensure_logbook_for_repeats()
+            self._sync_repeated_tasks_from_logbook()
+        return self._repeated_tasks
#+end_example

This should only be done on creation time, not on fetch time. Please ensure that the repeated tasks state is always synced on modification and can be returned directly.

*** DONE src/org_parser/document/_heading.py
:PROPERTIES:
:context: 
:END:
:LOGBOOK:
- State "DONE"       from "TODO"       [2026-03-18 Wed 23:17]
:END:

In file src/org_parser/document/_heading.py around line 413, near the ~new file   src/org_parser/document/_heading.py~

#+begin_example
+        self._dirty = True
+        if not self._parent.dirty:

#+end_example

This should just do =self._parent.mark_dirty()=.
*** DONE src/org_parser/document/_heading.py
:PROPERTIES:
:context: 
:END:
:LOGBOOK:
- State "DONE"       from "TODO"       [2026-03-18 Wed 23:04]
:END:

In file src/org_parser/document/_heading.py around line 391, near the ~new file   src/org_parser/document/_heading.py~

#+begin_example
+    def set_parent(
+        self,
+        value: Heading | Document,
+        *,
+        mark_dirty: bool = True,
+    ) -> None:
+        """Set parent object with optional dirty propagation."""
+        self._parent = value
+        if mark_dirty:
+            self._mark_dirty()

#+end_example

I don't like that this exists. Most of the call sites set the =mark_dirty= flag to =False=. Instead, the =parent= attribute should be used instead.

I'd like you to remove all instances of `set_parent`. All places that use it should now use the `parent` setter. Settnig a parent should not make an object dirty (parent does not participate in an objects representation). Setting children of an object should make it dirty.

*** DONE src/org_parser/element/__init__.py
:PROPERTIES:
:context: 
:END:
:LOGBOOK:
- State "DONE"       from "TODO"       [2026-03-18 Wed 22:50]
:END:

In file src/org_parser/element/__init__.py around line 45, near the ~new file   src/org_parser/element/__init__.py~

#+begin_example
+    "ListItemContinuation",

#+end_example

I'm pretty sure this isn't used anymore in the tree-sitter parser. Remove the whole =ListItemContinuation= class altogether.
*** DONE src/org_parser/element/_block.py
:PROPERTIES:
:context: 
:END:
:LOGBOOK:
- State "DONE"       from "TODO"       [2026-03-18 Wed 21:19]
:END:

In file src/org_parser/element/_block.py around line 49, near the ~new file   src/org_parser/element/_block.py~

#+begin_example
+        source_text: str = "",

#+end_example

This should be the owning =Document= insetad.
*** DONE src/org_parser/element/_block.py
:PROPERTIES:
:context: 
:END:
:LOGBOOK:
- State "DONE"       from "TODO"       [2026-03-19 Thu 01:16]
:END:

In file src/org_parser/element/_block.py around line 60, near the ~new file   src/org_parser/element/_block.py~

#+begin_example
+    def contents(self) -> list[Element]:
+        """Mutable block contents as semantic elements."""
+        return self._contents
#+end_example

This should be called =body= instead, to keep to the convention.
*** DONE src/org_parser/element/_element.py
:PROPERTIES:
:context: 
:END:
:LOGBOOK:
- State "DONE"       from "TODO"       [2026-03-19 Thu 14:54]
:END:

In file src/org_parser/element/_element.py around line 126, near the ~class Element:~

#+begin_example
+    def attach_backing(
#+end_example

The name is not great. Please rename to =attach_source=.
*** DONE src/org_parser/element/_block.py
:PROPERTIES:
:context: 
:END:
:LOGBOOK:
- State "DONE"       from "TODO"       [2026-03-18 Wed 21:19]
:END:

In file src/org_parser/element/_block.py around line 153, near the ~new file   src/org_parser/element/_block.py~

#+begin_example
+            node_type="center_block",

#+end_example

I don't like that all subclasses of =Element= pass it a node type. There's no need for that.
*** DONE src/org_parser/element/_block.py
:PROPERTIES:
:context: 
:END:
:LOGBOOK:
- State "DONE"       from "TODO"       [2026-03-18 Wed 22:34]
:END:

In file src/org_parser/element/_block.py around line 800, near the ~new file   src/org_parser/element/_block.py~

#+begin_example
+def _coalesce_list_items(elements: list[Element]) -> list[Element]:
+    """Recover semantic lists from flat nested block body elements."""
+    from org_parser.element._list_recovery import recover_lists
+
+    return recover_lists(elements, parent=None)
#+end_example

Looks like we don't need the wrapper method and could call the =recover_lists= function directly. Also, this sets the =List= objects parent to =None=.
*** DONE tests/test_block_elements.py
:PROPERTIES:
:context: 
:END:
:LOGBOOK:
- State "DONE"       from "TODO"       [2026-03-19 Thu 12:51]
:END:

In file tests/test_block_elements.py around line 42, near the ~if TYPE_CHECKING:~

#+begin_example
+    Blank-line nodes are wrapped as plain :class:`Element` instances (the base
+    class, not any concrete subclass), so an exact type check identifies them.
+    """
+    return [element for element in elements if type(element) is not Element]

#+end_example

A semantic node should be introduced for these.

*** DONE src/org_parser/element/_element.py
:PROPERTIES:
:context: 
:END:
:LOGBOOK:
- State "DONE"       from "TODO"       [2026-03-19 Thu 12:51]
:END:

In file src/org_parser/element/_element.py around line 157, near the ~def element_from_error_or_unknown(~

#+begin_example
+        paragraph = Paragraph(body=RichText(text), parent=parent)
+        paragraph.attach_backing(node, document)
+        return paragraph
+
+    elem = Element(parent=parent)
+    elem.attach_backing(node, document)
+    return elem
#+end_example

This should return a =RichText= element, not =Element= directly, even if the element is valid. This sholud be reported as an error in the document.
Please introduce a new semantic object for each of the valid nodes that appear in the parse tree and would otherwise result in a generic =Element=. Specifically, do that for the blank lines, etc.

*** DONE src/org_parser/element/_element.py
:PROPERTIES:
:context: 
:END:
:LOGBOOK:
- State "DONE"       from "TODO"       [2026-03-19 Thu 01:22]
:END:

In file src/org_parser/element/_element.py around line 214, near the ~new file   src/org_parser/element/_element.py~

#+begin_example

+def _is_error_node(node: tree_sitter.Node) -> bool:
+    """Return *True* if *node* is a parse-error or missing token.
+
+    Args:
+        node: Any tree-sitter node to inspect.
+
+    Returns:
+        ``True`` for ``ERROR``-typed nodes and for nodes where
+        ``node.is_missing`` is set by the parser's error-recovery.
+    """
+    return node.type == _ERROR_NODE_TYPE or node.is_missing
+
#+end_example

This looks useful enough, please extract it to common module and re-use wherever a check for the error node is made.
*** DONE src/org_parser/element/_drawer.py
:PROPERTIES:
:context: 
:END:
:LOGBOOK:
- State "DONE"       from "TODO"       [2026-03-19 Thu 00:31]
:END:

In file src/org_parser/element/_drawer.py around line 41, near the ~new file   src/org_parser/element/_drawer.py~

#+begin_example
+
+_PARAGRAPH = "paragraph"
+_ORG_TABLE = "org_table"
+_TABLEEL_TABLE = "tableel_table"
+_CLOCK = "clock"
+_DRAWER = "drawer"
+_LOGBOOK_DRAWER = "logbook_drawer"
+_PROPERTY_DRAWER = "property_drawer"
+_NODE_PROPERTY = "node_property"
+_CENTER_BLOCK = "center_block"
+_QUOTE_BLOCK = "quote_block"
+_SPECIAL_BLOCK = "special_block"
+_DYNAMIC_BLOCK = "dynamic_block"
+_COMMENT_BLOCK = "comment_block"
+_EXAMPLE_BLOCK = "example_block"
+_EXPORT_BLOCK = "export_block"
+_SRC_BLOCK = "src_block"
+_VERSE_BLOCK = "verse_block"
+_FIXED_WIDTH = "fixed_width"
+_LIST_ITEM = "list_item"
+_BLOCK = "block"
#+end_example

These seem to be duplicated all over the place. Instead these should be defined once and imported. A good place for these definitions would be the nodes module.

Please also move the planning keywords constants there.

When importing these constants, please avoid renaming the import - it makes sense to just import these and use as-is.
*** DONE src/org_parser/element/_drawer.py
:PROPERTIES:
:context: 
:END:
:LOGBOOK:
- State "DONE"       from "TODO"       [2026-03-18 Wed 21:19]
:END:

In file src/org_parser/element/_drawer.py around line 81, near the ~new file   src/org_parser/element/_drawer.py~

#+begin_example
+        source_text: str = "",

#+end_example

Again, use =Document= instead of passing =source_text= around.
*** DONE src/org_parser/text/_rich_text.py
:PROPERTIES:
:context: 
:END:
:LOGBOOK:
- State "DONE"       from "TODO"       [2026-03-19 Thu 20:14]
:END:

In file src/org_parser/text/_rich_text.py around line 189, near the ~new file   src/org_parser/text/_rich_text.py~

#+begin_example
        source: bytes,
+
#+end_example

Should use the =document.source= instead of =source: bytes=.
*** DONE src/org_parser/element/_drawer.py
:PROPERTIES:
:context: 
:END:
:LOGBOOK:
- State "DONE"       from "TODO"       [2026-03-18 Wed 22:34]
:END:

In file src/org_parser/element/_drawer.py around line 439, near the ~new file   src/org_parser/element/_drawer.py~

#+begin_example
+def _coalesce_list_items(
+    elements: list[Element],
+    *,
+    parent: Document | Heading | Element | None,
+) -> list[Element]:
+    """Recover semantic lists from flat drawer body elements."""
+    return recover_lists(elements, parent=parent)
#+end_example

No need for the wrapper, use the =recover_lists= directly.
*** DONE src/org_parser/element/_table.py
:PROPERTIES:
:context: 
:END:
:LOGBOOK:
- State "DONE"       from "TODO"       [2026-03-19 Thu 17:57]
:END:

In file src/org_parser/element/_table.py around line 247, near the ~new file   src/org_parser/element/_table.py~

#+begin_example
+    """Parse one ``table_row`` node into :class:`TableRow`."""
+    has_rule = any(child.type == "table_rule" for child in node.named_children)
+    if has_rule:
+        return TableRow(cells=[], is_rule=True, table=table)

#+end_example

Kinda makes sense to store the contents of the rule too. Please introduce a dedicated =TableRuleRow= class that stores the entire rule. It should replace the current =is_rule= flag on the row.

*** DONE src/org_parser/element/_element.py
:PROPERTIES:
:context: 
:END:
:LOGBOOK:
- State "DONE"       from "TODO"       [2026-03-18 Wed 21:19]
:END:

In file src/org_parser/element/_element.py around line 76, near the ~new file   src/org_parser/element/_element.py~

#+begin_example
        node_type: str = "",
+        source_text: str = "",
+
#+end_example

No need for the =node_type=, just accept the =document= as the owner and use that for the default representation.
*** CANCELLED src/org_parser/element/_list.py
:PROPERTIES:
:context: 
:END:
:LOGBOOK:
- State "CANCELLED"  from "TODO"       [2026-03-19 Thu 17:39] \\
  Will be done at a later point.
:END:

In file src/org_parser/element/_list.py around line 626, near the ~new file   src/org_parser/element/_list.py~

#+begin_example
+    """Return bullet marker text from one list item node."""
+    bullet_nodes = node.children_by_field_name("bullet")
+    if not bullet_nodes:
+        return "-"

#+end_example

Not currently on the tree-sitter parser, but will be added.
*** DONE src/org_parser/element/_element.py
:PROPERTIES:
:context: 
:END:
:LOGBOOK:
- State "DONE"       from "TODO"       [2026-03-18 Wed 21:19]
:END:

In file src/org_parser/element/_element.py around line 94, near the ~new file   src/org_parser/element/_element.py~

#+begin_example
    @classmethod
+    def from_node(
+        cls,
+        node: tree_sitter.Node,
+        document: Document | None = None,
+        *,
+        parent: Document | Heading | Element | None = None,
+    ) -> Element:
+
#+end_example

This shouldn't ever be used. This class should just be a base class for all the other elements.
*** DONE src/org_parser/element/_list.py
:PROPERTIES:
:context: 
:END:
:LOGBOOK:
- State "DONE"       from "TODO"       [2026-03-18 Wed 22:51]
:END:

In file src/org_parser/element/_list.py around line 38, near the ~new file   src/org_parser/element/_list.py~

#+begin_example
+class ListItemContinuation(Element):

#+end_example

This doesn't happen in the tree-sitter parser anymore.
*** DONE Repeat note
:LOGBOOK:
- State "DONE"       from "TODO"       [2026-03-19 Thu 15:40]
:END:
Please refactor the =Repeat= parsing to use the =first_line= from a =ListItem= for structure (assuming there's no =item_tag= or =counter_set= or =checkbox= etc). Use the =body= for the repeat note.

The =Repeat= parsing should be done *after* the list reconstruction, so that we can make sure that the note is a part of the list item.

*** DONE src/org_parser/element/_list.py
:PROPERTIES:
:context: 
:END:
:LOGBOOK:
- State "DONE"       from "TODO"       [2026-03-19 Thu 15:40]
:END:

In file src/org_parser/element/_list.py around line 358, near the ~class Repeat(ListItem):~

#+begin_example
     def from_list_item(cls, item: ListItem) -> Repeat | None:
         """Build a :class:`Repeat` from one list item when pattern-matched."""
-        matched = _parse_repeat_source(item.source_text)
+        node = item._node
+        doc = item._document
+        item_source = (
+            doc.source[node.start_byte : node.end_byte].decode()
+            if node is not None and doc is not None
+            else ""
+        )
+        matched = _parse_repeat_source(item_source)
#+end_example

I would rather this used the =item.first_line= instead of using the source directly.
*** DONE src/org_parser/element/_list.py
:PROPERTIES:
:context: 
:END:
:LOGBOOK:
- State "DONE"       from "TODO"       [2026-03-19 Thu 15:40]
:END:

In file src/org_parser/element/_list.py around line 722, near the ~new file   src/org_parser/element/_list.py~

#+begin_example
+@dataclass(slots=True)
+class _RepeatMatch:
+    """Parsed repeated-task fields extracted from one list item text."""
+
+    after: str
+    before: str
+    timestamp: Timestamp
+    note: str | None
+    note_indent: str
#+end_example

Check if this would be more natural in tree-sitter instead.
*** DONE Indent recovery refactor
:LOGBOOK:
- State "DONE"       from "TODO"       [2026-03-19 Thu 16:21]
:END:
=ListItem= and =Repeat= should not have a =line_prefix=. They should dirty-stringify their bodies to align them to the line text start, i.e a space "after" the bullet column:

#+begin_example
- List item line
  body is aligned to the line text
#+end_example

Remove the indentation handling from =List= - since =ListItem= already handles indentation of its body it should be possible to just stringify each item of a list and len them handle any nested lists.

Similarily, after structure recovery the =Paragraphs= should already be well-formed and embedded in structured lists, so there is no need for any extra indent handling.

*** DONE src/org_parser/element/_list.py
:PROPERTIES:
:context: 
:END:
:LOGBOOK:
- State "DONE"       from "TODO"       [2026-03-19 Thu 16:19]
:END:

In file src/org_parser/element/_list.py around line 294, near the ~new file   src/org_parser/element/_list.py~

#+begin_example
+        indent = _extract_leading_indent(source_text)
+        normalized_indent = indent if indent != "" else None
+        self._note_indent = (
+            note_indent
+            if note_indent is not None
+            else _default_note_indent(normalized_indent)
+        )
#+end_example

This should instead be handled by the indent block logic.
*** DONE src/org_parser/element/_list_recovery.py
:PROPERTIES:
:context: 
:END:
:LOGBOOK:
- State "DONE"       from "TODO"       [2026-03-19 Thu 17:32]
:END:

In file src/org_parser/element/_list_recovery.py around line 125, near the ~new file   src/org_parser/element/_list_recovery.py~

#+begin_example
+    item_indent = base_indent + _source_indent_width(item.source_text)
+    block_indent = _source_indent_width(block.source_text)
#+end_example

This should be based on the =IndentBlock=, however I see that the =IndentBlock= currently doesn't have an =indent= attribute that is set on the parse tree. Please update that class to have an indent that then can be used for comparison in list recovery.

The tree sitter parser ensures that indent blocks are nested - we can assume that all elements within the same block are at the same indent level. Whenever running the structure recovery, check for paragraph/indent block levels and coalesce conecutive blocks/paragraphs that match the indent level. If recovering list structure, attach any blocks/paragraphs that are more indented that the current list item to that item as a body element.

*** DONE src/org_parser/document/_body.py
:PROPERTIES:
:context: 
:END:
:LOGBOOK:
- State "DONE"       from "TODO"       [2026-03-19 Thu 17:32]
:END:

In file src/org_parser/document/_body.py around line 146, near the ~def extract_indent_block(~

#+begin_example
+    indent = (
+        source[indent_node.start_byte : indent_node.end_byte].decode()
+        if indent_node is not None
+        else None
+    )
#+end_example

This should use the =node_source= helper function.

*** DONE src/org_parser/element/_paragraph.py
:PROPERTIES:
:context: 
:END:
:LOGBOOK:
- State "DONE"       from "TODO"       [2026-03-19 Thu 17:38]
:END:

In file src/org_parser/element/_paragraph.py around line 121, near the ~new file   src/org_parser/element/_paragraph.py~

#+begin_example
+def _extract_indent(node: tree_sitter.Node, source: bytes) -> str | None:
+    """Return paragraph first-line indentation, if available."""
+    field_node = node.child_by_field_name("indent")
+    if field_node is not None:
+        value = source[field_node.start_byte : field_node.end_byte].decode()
+        return value if value != "" else None
+
+    paragraph_text = source[node.start_byte : node.end_byte].decode()
+    first_line, _, _ = paragraph_text.partition("\n")
+    indent_end = 0
+    while indent_end < len(first_line) and first_line[indent_end] in {" ", "\t"}:
+        indent_end += 1
+    if indent_end == 0:
+        return None
+    return first_line[:indent_end]
#+end_example

Any indented paragraph will have an indent field, however, when the indent is not avaialble, thre is no indent, o the latter half of the function will likely extract nothing.

*** DONE src/org_parser/element/_list.py
:PROPERTIES:
:context: 
:END:
:LOGBOOK:
- State "DONE"       from "TODO"       [2026-03-19 Thu 16:19]
:END:

In file src/org_parser/element/_list.py around line 338, near the ~class ListItem(Element):~

#+begin_example
         default_indent = self._line_prefix if self._line_prefix != "" else None
         return self.render_with_indent(default_indent)
#+end_example

This is not needed, when building a new representation, just use the default indent.
*** DONE Code review comment
:PROPERTIES:
:context: 
:END:
:LOGBOOK:
- State "DONE"       from "TODO"       [2026-03-19 Thu 16:28]
:END:

Filename: 

#+begin_example
    def render_with_indent(self, indent: str | None, *, indent_step: int = 2) -> str:
        """Render list-item text with one explicit indentation prefix."""
        parts: list[str] = []
        if indent is not None:
            parts.append(indent)

        if self._ordered_counter is not None and self._bullet in {".", ")"}:
            parts.append(f"{self._ordered_counter}{self._bullet} ")
        else:
            parts.append(f"{self._bullet} ")

        if self._counter_set is not None:
            parts.append(f"[@{self._counter_set}] ")

        if self._checkbox is not None:
            parts.append(f"[{self._checkbox}] ")

        if self._item_tag is not None:
            parts.append(f"{self._item_tag} ::")
        elif self._first_line is not None:
            parts.append(str(self._first_line))

        parts.append("\n")
        body_prefix = (indent if indent is not None else "") + (" " * indent_step)
        for element in self._body:
            rendered = ensure_trailing_newline(str(element))
            if element.node_type == "plain_list":
                parts.append(rendered)
                continue
            parts.append(_indent_non_empty_lines(rendered, body_prefix))
        return "".join(parts)

#+end_example

This should be recursive and ensure the well-formedness of the nested elements. I.e. =str(el)= and then apply indent to all the output lines.
*** CANCELLED src/org_parser/text/_rich_text.py
:PROPERTIES:
:context: 
:END:
:LOGBOOK:
- State "CANCELLED"  from "TODO"       [2026-03-18 Wed 23:10] \\
  It is used in places.
:END:

In file src/org_parser/text/_rich_text.py around line 112, near the ~new file   src/org_parser/text/_rich_text.py~

#+begin_example

+    @property
+    def dirty(self) -> bool:
+        """Whether this rich text has been mutated after creation."""
+        return self._dirty
+
#+end_example

Doesn't sound like it's needed.
*** DONE src/org_parser/text/_rich_text.py
:PROPERTIES:
:context: 
:END:
:LOGBOOK:
- State "DONE"       from "TODO"       [2026-03-18 Wed 22:26]
:END:

In file src/org_parser/text/_rich_text.py around line 459, near the ~new file   src/org_parser/text/_rich_text.py~

#+begin_example
def _safe_node_text(node: tree_sitter.Node | None, source: bytes) -> str:
+    """Return node text or an empty string for missing nodes."""
+    if node is None:
+        return ""
+    return _node_text(node, source)
+
+
+def _node_text(node: tree_sitter.Node, source: bytes) -> str:
+    """Return source text covered by one node."""
+    return source[node.start_byte : node.end_byte].decode()

#+end_example

Not great that these two exist side-by-side.
*** CANCELLED src/org_parser/text/_rich_text.py
:PROPERTIES:
:context: 
:END:
:LOGBOOK:
- State "CANCELLED"  from "TODO"       [2026-03-19 Thu 17:58] \\
  These are reasonable.
:END:

In file src/org_parser/text/_rich_text.py around line 291, near the ~new file   src/org_parser/text/_rich_text.py~

#+begin_example
+def _parse_inline_node(  # noqa: PLR0911,PLR0912,PLR0915
#+end_example

Check all the linter exclusions.
*** DONE src/org_parser/time/_timestamp.py
:PROPERTIES:
:context: 
:END:
:LOGBOOK:
- State "DONE"       from "TODO"       [2026-03-18 Wed 22:31]
:END:

In file src/org_parser/time/_timestamp.py around line 192, near the ~new file   src/org_parser/time/_timestamp.py~

#+begin_example
+def _node_text(node: tree_sitter.Node, source: bytes) -> str:
+    """Return source text covered by one node."""
+    return source[node.start_byte : node.end_byte].decode()

#+end_example

This is another candidate for a generic helper function.
*** CANCELLED src/org_parser/document/_body.py
:PROPERTIES:
:context: 
:END:
:LOGBOOK:
- State "CANCELLED"  from "TODO"       [2026-03-18 Wed 21:22] \\
  It's fine.
:END:

In file src/org_parser/document/_body.py around line 207, near the ~def extract_indent_block(~

#+begin_example
+    block.attach_backing(node, document)

#+end_example

This is wack, no need for a dedicated function for that.
*** DONE src/org_parser/element/_block.py
:PROPERTIES:
:context: 
:END:
:LOGBOOK:
- State "DONE"       from "TODO"       [2026-03-18 Wed 22:26]
:END:

In file src/org_parser/element/_block.py around line 692, near the ~class FixedWidthBlock(Element):~

#+begin_example
+            return self._document.source[
+                self._node.start_byte : self._node.end_byte
+            ].decode()

#+end_example

There's some duplication when fetching source.
*** CANCELLED src/org_parser/element/_block.py
:PROPERTIES:
:context: 
:END:
:LOGBOOK:
- State "CANCELLED"  from "TODO"       [2026-03-19 Thu 17:59] \\
  w/e
:END:

In file src/org_parser/element/_block.py around line 936, near the ~def _ensure_single_trailing_newline(value: str) -> str:~

#+begin_example
-    return f"{value.rstrip('\n')}\n"
+    return f"{value.rstrip(chr(10))}\n"

#+end_example

???
*** DONE src/org_parser/element/_drawer.py
:PROPERTIES:
:context: 
:END:
:LOGBOOK:
- State "DONE"       from "TODO"       [2026-03-18 Wed 22:26]
:END:

In file src/org_parser/element/_drawer.py around line 301, near the ~class Properties(Element, MutableMapping[str, RichText]):~

#+begin_example
+            node = element._node
+            doc = element._document
+            raw = (
+                doc.source[node.start_byte : node.end_byte].decode()
+                if node is not None and doc is not None
+                else ""
+            )
+            line = raw.rstrip("\n")

#+end_example

This whole function is weird, need to check if that is required.
*** DONE src/org_parser/time/_timestamp.py
:PROPERTIES:
:context: 
:END:
:LOGBOOK:
- State "DONE"       from "TODO"       [2026-03-19 Thu 00:51]
:END:

In file src/org_parser/time/_timestamp.py around line 161, near the ~class Timestamp:~

#+begin_example
         object.__setattr__(self, "_dirty", True)
#+end_example

This is weird. I would rather that the Timestamp were made mutable and supported setters.

*** DONE src/org_parser/element/_block.py
:PROPERTIES:
:context: 
:END:
:LOGBOOK:
- State "DONE"       from "TODO"       [2026-03-19 Thu 13:50]
:END:

In file src/org_parser/element/_block.py around line 779, near the ~def _extract_nested_element(~

#+begin_example
         VERSE_BLOCK: VerseBlock.from_node,
         FIXED_WIDTH: FixedWidthBlock.from_node,
         LIST_ITEM: ListItem.from_node,
+        BLANK_LINE: BlankLine.from_node,
+        CAPTION_KEYWORD: CaptionKeyword.from_node,
+        COMMENT: Comment.from_node,
+        HORIZONTAL_RULE: HorizontalRule.from_node,
     }
     factory = dispatch.get(node.type)
#+end_example

Sounds like these dispatch tables could be extracted to a helper function as they appear to be the same.
*** DONE src/org_parser/element/_misc.py
:PROPERTIES:
:context: 
:END:
:LOGBOOK:
- State "DONE"       from "TODO"       [2026-03-19 Thu 13:18]
:END:

In file src/org_parser/element/_misc.py around line 179, near the ~new file   src/org_parser/element/_misc.py~

#+begin_example
+
+class CaptionKeyword(Element):
+    """A ``#+CAPTION:`` affiliated keyword line.
#+end_example

Check if the other body keywords are handled as well: RESULTS, TABLENAME, PLOT, etc.
*** DONE src/org_parser/element/_drawer.py
:PROPERTIES:
:context: 
:END:
:LOGBOOK:
- State "DONE"       from "TODO"       [2026-03-20 Fri 00:44]
:END:

In file src/org_parser/element/_drawer.py around line 66, near the ~class Drawer(Element):~

#+begin_example
+        source: bytes = (document.source or b"") if document is not None else b""
#+end_example

This seems to be a common pattern now in almost every =from_node= implementation. I wonder if we could refactor this by introducing a method =Document.source_for(node)= that would return the source or =None= if source is not available - it makes no sense to call a =from_node= with a document that doesn't have a source, so ideally each =from_node= would detect that and return =None= as well.

Please refactor the various =_extract_*= functions to receive the document and use this new method to extract the source code for further processing.

*** DONE Source handling refactor
:LOGBOOK:
- State "DONE"       from "TODO"       [2026-03-20 Fri 12:21]
:END:
Refactor all factory methods =from_node= to require a =Document= argument. It shound never be =None=.
Remove the =source= property from =Document= and its setter. Only the =source_for= method should be used.

Remove the duplicate =_source_for_document= function, these should not be used. Refactor all places that require that function to unstead use =Document.source_for=.

Avoid using =node.text= from tree-sitter. Only the =document.source_for= method should be used.

Don't catch the error that =document.source_for= might throw.

Remove the =_node_text_for_document= function. =document.source_for= should suffice.

The =_parse_inline_nodes= function should also receive the whole document instead of plain source.

*** DONE Code review comment
:LOGBOOK:
- State "DONE"       from "TODO"       [2026-03-20 Fri 12:21]
:END:

Filename: /home/k/projects/software/org-mode-parser/src/org_parser/element/_drawer.py

#+begin_example
def _source_for_document(document: Document | None, *, context: str) -> bytes:
    """Return full document source bytes or raise for source-less documents."""
    if document is None:
        return b""
    source = document.source_for()
    if source is None:
        raise ValueError(f"{context} requires document source bytes")
    return source
#+end_example

This function is duplicated in numerous modules. I don't like that it exists - usually the source is extracted so that it can be indexed directly with a node start & end bytes' offest. Instead, all those places should use =document.source_for=. Please refactor all usages of these =_source_for_document= functions. I would like =document.source_for= to always accept a node. No =None= value, no default argument value.

*** DONE src/org_parser/_node.py
:PROPERTIES:
:context: 
:END:
:LOGBOOK:
- State "DONE"       from "TODO"       [2026-03-20 Fri 12:19]
:END:

In file src/org_parser/_node.py around line 77, near the ~def node_source(node: tree_sitter.Node | None, document: Document | None) -> str~

#+begin_example
-    if source is None:
+    try:
+        source = document.source_for(node)
+    except ValueError:

#+end_example

Wack
*** DONE src/org_parser/document/_heading.py
:PROPERTIES:
:context: 
:END:
:LOGBOOK:
- State "DONE"       from "TODO"       [2026-03-20 Fri 12:19]
:END:

In file src/org_parser/document/_heading.py around line 660, near the ~def _extract_body(~

#+begin_example
     """Return ``(scheduled, deadline, closed)`` planning timestamps."""
+    source = document.source
+    if source is None:
+        raise ValueError("Heading planning extraction requires document source bytes")

#+end_example

Please use the =source_for= method instead of getting the source directly.
*** DONE src/org_parser/element/_block.py
:PROPERTIES:
:context: 
:END:
:LOGBOOK:
- State "DONE"       from "TODO"       [2026-03-20 Fri 12:19]
:END:

In file src/org_parser/element/_block.py around line 769, near the ~def _extract_indent_block(~

#+begin_example
+    if document is None:
+        value = field_node.text.decode() if field_node.text is not None else ""

#+end_example

Should it use the tree-sitter node =text=?
*** DONE src/org_parser/element/_drawer.py
:PROPERTIES:
:context: 
:END:
:LOGBOOK:
- State "DONE"       from "TODO"       [2026-03-20 Fri 12:20]
:END:

In file src/org_parser/element/_drawer.py around line 75, near the ~class Drawer(Element):~

#+begin_example
+        elif document is None:
+            name = name_node.text.decode() if name_node.text is not None else ""

#+end_example

=from_node= methods should require the document to be present.
*** DONE src/org_parser/element/_list.py
:PROPERTIES:
:context: 
:END:
:LOGBOOK:
- State "DONE"       from "TODO"       [2026-03-20 Fri 12:21]
:END:

In file src/org_parser/element/_list.py around line 509, near the ~class List(Element):~

#+begin_example
-    value = node_text(field_node, source)
+    value = _node_text_for_document(field_node, document)

#+end_example

Should not be needed.
*** DONE src/org_parser/element/_list.py
:PROPERTIES:
:context: 
:END:
:LOGBOOK:
- State "DONE"       from "TODO"       [2026-03-20 Fri 12:21]
:END:

In file src/org_parser/element/_list.py around line 644, near the ~def _normalize_optional_text(value: str | None) -> str | None:~

#+begin_example

+def _node_text_for_document(
+    node: tree_sitter.Node | None,
+    document: Document | None,
+) -> str:
+    """Return node text using document source when available."""
+    if node is None:
+        return ""
+    if document is None:
+        return node.text.decode() if node.text is not None else ""
+    return document.source_for(node).decode()

#+end_example

This whole function should not exist.
*** DONE src/org_parser/text/_rich_text.py
:PROPERTIES:
:context: 
:END:
:LOGBOOK:
- State "DONE"       from "TODO"       [2026-03-20 Fri 12:21]
:END:

In file src/org_parser/text/_rich_text.py around line 195, near the ~class RichText:~

#+begin_example
             parts = _parse_inline_nodes(node.named_children, source, document)

#+end_example

This should also receive the whole document rather than source.
*** DONE Code review comment
:PROPERTIES:
:context: [[file:~/projects/software/org-mode-parser/src/org_parser/element/_list.py::parsed = _parse_repeat_first_line(str(item.first_line))]]
:END:
:LOGBOOK:
- State "DONE"       from "TODO"       [2026-03-20 Fri 12:35]
:END:

Filename: /home/k/projects/software/org-mode-parser/src/org_parser/element/_list.py

#+begin_example
        parsed = _parse_repeat_first_line(str(item.first_line))
#+end_example

This should take the rich text and analyze its parts so that we can avoid weird comparisons between tree sitter nodes and extracted timestamp strings. Please refactor this function to take the first line as rich text, then check for the plaintext item followed by a timestamp and the remainder and use those parts for the parsing.

*** DONE src/org_parser/element/_list.py
:PROPERTIES:
:context: 
:END:
:LOGBOOK:
- State "DONE"       from "TODO"       [2026-03-20 Fri 12:35]
:END:

In file src/org_parser/element/_list.py around line 307, near the ~class Repeat(ListItem):~

#+begin_example
+        timestamp = _extract_repeat_timestamp(item, timestamp_text)
+        if timestamp is None:
#+end_example

This is weird. Ideally would be done in one go by using the timestamp node directly.

*** DONE src/org_parser/document/_document.py
:PROPERTIES:
:context: 
:END:
:LOGBOOK:
- State "DONE"       from "TODO"       [2026-03-20 Fri 18:10]
:END:

In file src/org_parser/document/_document.py around line 334, near the ~class Document:~

#+begin_example
+    @property
+    def node(self) -> tree_sitter.Node | None:
+        """Root parse node that backs this document, when present."""
+        return self._node
#+end_example

Shouldn't be needed.
*** CANCELLED src/org_parser/element/_list.py
:PROPERTIES:
:context: 
:END:
:LOGBOOK:
- State "CANCELLED"  from "TODO"       [2026-03-20 Fri 18:18] \\
  This would make it inconvenient to build the tree.
:END:

In file src/org_parser/element/_list.py around line 77, near the ~class ListItem(Element):~

#+begin_example
         parent: Document | Heading | Element | None = None,
#+end_example

Parent should be mandatory in all =from_node= implementations - if there's a tree there's a parent/child relationship.

*** DONE src/org_parser/text/_rich_text.py
:PROPERTIES:
:context: 
:END:
:LOGBOOK:
- State "DONE"       from "TODO"       [2026-03-20 Fri 17:41]
:END:

In file src/org_parser/text/_rich_text.py around line 198, near the ~class RichText:~

#+begin_example
+        root = document.node
+        if root is None:
+            raise ValueError("RichText.from_node requires document parse node")
+        source = document.source_for(root)

#+end_example

=RichText= should not store the source and instead store the document. I would rather it stored just the source representing this text element.

*** DONE Code review comment
:LOGBOOK:
- State "DONE"       from "TODO"       [2026-03-20 Fri 18:18]
:END:
:PROPERTIES:
:context: [[file:~/projects/software/org-mode-parser/src/org_parser/text/_rich_text.py::@classmethod
 def from_node(
 cls,
 node: tree_sitter.Node,
 *,
 document: Document,
 ) -> RichText:
 """Create a :class:`RichText` from a single tree-sitter node.]]
:END:

Filename: /home/k/projects/software/org-mode-parser/src/org_parser/text/_rich_text.py

#+begin_example
    @classmethod
    def from_node(
        cls,
        node: tree_sitter.Node,
        *,
        document: Document,
    ) -> RichText:
        """Create a :class:`RichText` from a single tree-sitter node.
#+end_example

=RichText.from_node= is missing the optional parent. Please add that. Do add the parameter to =from_nodes= as well. Please also update the call-sites of =RichText.from_node= and =RichText.from_nodes= to include the parent node.

*** DONE AI review comments
:LOGBOOK:
- State "DONE"       from "TODO"       [2026-03-20 Fri 19:27]
:END:
Please fix the following issues:

1. =_timestamp.py= - =is_explicit_range= only compares ~end_day != start_day~, so a cross-month range like =<2024-01-15>--<2024-02-15>= is misclassified as a same-day time range (same day number, different month), potentially corrupting rendered output.

2. =_list.py:603,607= - Line break detection uses ="\\\\n"= (the 4-char literal =\\n=) instead of ="\\\\"= (two backslashes), which is what =LineBreak.__str__= actually produces. The repeat-note splitting logic would never match in normal usage.

3. =_list_recovery.py= - Tabs counted as width 1 instead of 8, breaking indent comparison in mixed-indent documents.

4. =_clock.py= - Missing =__repr__= on =Clock=

5. =_rich_text.py:197= - Wrong return type annotation on =RichText.from_nodes=. The method is annotated =-> RichText | None= but never returns =None=. This causes callers in =_table.py= to have an unreachable =value is None= branch, and worse, they re-wrap the result as =RichText(str(value).strip())=, discarding source-byte preservation for table cells.

*** DONE Repeats with a note issue
Given this example:
#+begin_example
:LOGBOOK:
:LOGBOOK:
- State "DONE"       from "TODO"       [2026-03-20 Fri 21:51]
:END:
- State "CANCELLED"  from "TODO"       [2025-07-28 pon 18:41] \\
  No need to.
- State "DONE"       from "TODO"       [2025-07-28 pon 18:33]
- State "DONE"       from "TODO"       [2025-07-26 sob 22:18]
- State "DONE"       from "TODO"       [2025-06-29 nie 20:54]
- State "DONE"       from "TODO"       [2025-06-26 czw 22:39]
- State "CANCELLED"  from "TODO"       [2025-06-20 pią 19:54] \\
  Missed it.
- State "CANCELLED"  from "TODO"       [2025-06-20 pią 19:54] \\
  Note
:END:
#+end_example

The repeat recovery should recognize 7 repeats, four that don't have a note and three that do. Currently, it only recognizez the four that don't have a note. Notice the newline charecters following the line-break.

Please update the implementation to make sure that tese do work correctly. Please also update the existing tests that don't have a newline to make more sense - when there are trailing characters following the linebreak it is currently parsed as a plaintext item. In such a case, when the first line of a list item does not end with a line break, please include the trailing data in the note along with the body (if one follows).

Stringification of a repeat should also include the trailing characters on the first line and the body as a note.

** DONE Blank line handling
:LOGBOOK:
- State "DONE"       from "TODO"       [2026-03-20 Fri 23:08]
:END:
There is a medium-severity bug in =src/org_parser/element/_list_recovery.py:58= where blank lines are dropped whenever a list run is open outside an indent block.

Parsing =- a\n\nPara\n=, then making the doc dirty, serializes as =- a\nPara\n= (blank line lost).

Root cause: =BlankLine= is intentionally ignored while =list_run= is active (=src/org_parser/element/_list_recovery.py:58=), so no separator survives into dirty rendering.

* DONE Parser fidelity
:LOGBOOK:
- State "DONE"       from "TODO"       [2026-03-21 Sat 23:56]
:END:
** DONE Special handling for LOGBOOK
:LOGBOOK:
- State "DONE"       from "TODO"       [2026-03-08 Sun 17:50]
:END:
The =LOGBOOK= drawer should be treated in the same way as the =PROPERTIES= drawer, i.e. it should have a dedicated parse tree node associated with it. Other custom drawers can be left as-is.

** DONE Bind indent in lesser/greater elements
:LOGBOOK:
- State "DONE"       from "TODO"       [2026-03-09 Mon 22:01]
:END:
Update the parser to bind indent level in elements such as drawers, blocks, paragraphs and all list items. Bind it even if the indent level is 0. It's sufficient to bind the indent of the beginning of the element.
This will be used by semantic analysis to recover list nesting.

*** COMMENT
The indent is still not added to the beginning of an object such as a drawer when it is following a list item.

** CANCELLED Heading line abstraction
:LOGBOOK:
- State "CANCELLED"  from "TODO"       [2026-03-08 Sun 13:18] \\
  No need for that with the semantic nodes.
:END:
Add an abstraction over the heading line, so that the heading consists of a =heading_line= and a =body=.

** DONE Priority value abstraction in the parser
:LOGBOOK:
- State "DONE"       from "TODO"       [2026-03-08 Sun 13:56]
:END:
Priority values are not bound in the parser, but should for easier extraction of the value:

#+begin_example
,* TODO [#A] Test
#+end_example

In the above example, it would be ideal if the parser extracted the value =A= and bound it as a subnode of the priority node under the name =value=.

Please update the semantic nodes to use that extracted value instead.

** DONE Counter value abstraction in the parser
:LOGBOOK:
- State "DONE"       from "TODO"       [2026-03-08 Sun 14:07]
:END:
Completion counter values are not bound in the parser, but should for easier extraction of the value:

#+begin_example
,* TODO Test [1/2]
#+end_example

In the above example, it would be ideal if the parser extracted the value =1/2= and bound it as a subnode of the counter node under the name =value=.

Please update the semantic nodes to use that extracted value instead.

** DONE Dashes in #+TODO states
:LOGBOOK:
- State "DONE"       from "TODO"       [2026-03-08 Sun 13:44]
:END:
The value =IN-PROGRESS= doesn't parse as a todo state when configured, but should.

#+begin_example
#+TODO: TODO IN-PROGRESS | DONE
,* TODO Test
,* DONE test
,* IN-PROGRESS Test
#+end_example

Adjust the parse rules for todo states to allow for =-= and =_= in state names.

** DONE Multiline paragraphs don't preserve newlines
:LOGBOOK:
- State "DONE"       from "TODO"       [2026-03-09 Mon 18:50]
:END:
The following example ends up as three separate plaintext lines separated by newlines, but none of these newlines is captured.

#+begin_example
This is some text:
More text
More text
#+end_example

Ideally, the paragraph would still span multiple lines, but would capture the newlines properly. The newline character can be a part of each line.
*** COMMENT
Initially, Opus blew through all the quota trying to figure out what could be done and produced an unsatisfactory plan.
Then, Codex was able to reword the plan and begin execution, however, there were issues with API communication with OpenAI, so Codex couldn't finish implementing the plan. Sonnet continued the implementation, but decided to make newlines a separate plaintext item, which is fine. In the end, Sonnet reverted all changes and instead implemented a work-around in Python by checking for the missing newline characters.

** DONE List vs List item indent problems
:LOGBOOK:
- State "DONE"       from "TODO"       [2026-03-10 Tue 10:15]
:END:
We recently introduced blanks and indentation to the parse tree.
With those values we could optimize the flat list representation in the parse tree to not impose a structure of a =plain_list= with embedded =items=. Instead, each item should become a =list_item= that lives separately and has an indent associated with it.

The rules for parsing these items remain the same. The rules for =plain_list= should be removed. Pay special attention to attach the indent to the elements following a =list_item= - =list_item= should end at a newline boundary when followed by a non-text element such as a drawer or a block. If that following element is indented, the indent should be attached to that element.

There's a regression when parsing a list that is following a heading and a drawer:
#+begin_example
,** Unreleased
:PROPERTIES:
:CUSTOM_ID: unreleased
:END:

- IN-PROGRESS Add ~OBJECT FREQ~ command for LFU policy introspection. :feature:
- TODO Implement ~LMPOP~ and ~ZMPOP~ for multi-key blocking pops. :feature:
- TODO Structured JSON logging via =tracing-subscriber= JSON formatter. :observability:
#+end_example

Please make sure to fix the issue.

Another regression:
#+begin_example
- [X] Javadoc documentation
- [X] Logging:
  + [X] Competitor
  + [X] Shooting range
- [X] UML diagrams:
#+end_example

Looks like lists do not recognize the bullets correctly. Please make sure that all the bullets are recognized correctly for unordered and ordered lists. Make sure that =list_item= bindgs the bullet used for the list.

Make sure to add corpus tests for all of these regressions.

*** COMMENT
Somehow, fixing the bullet issue was really hard for Codex, it attempted two "solutions" breaking more tests in the process and it reverted the code twice calling the second attempt as "fixed".

Another session also failed to fix the issue and instead removed the test case from the corpus.

Finally, the third time I decided to get rid of the item continuation lines altogether, which allowed the AI to actually make something working, except that now it parses =+= as =repeat_marker=. Still to do:

- List reconstruction doesn't properly add bodies that start with an indent.
- =- + foo= in lists is causing an error (markup.txt test file was updated to get that in)
- some of the archive files don't parse now
- parser doesn't bind the bullet used for the list

** DONE Indent on table formula
:LOGBOOK:
- State "DONE"       from "TODO"       [2026-03-10 Tue 10:30]
:END:
Here's a new edge case that popped up:
#+begin_example
 |    *Total* |              |            |                 |                |              150 |
 #+TBLFM: @>$6=vsum(@2$6..@>>$6)
#+end_example
The TBLFM line is indented and the indentation causes an error. Please add a test case for this and fix the issue.

** DONE =+= in list/headings
:LOGBOOK:
- State "DONE"       from "TODO"       [2026-03-10 Tue 10:43]
:END:
I found some more edge cases all related to the =+= character and lists:

#+begin_example
,*** TODO + in list/headings
#+end_example
Here the initial =+= character of the heading line causes an issue - it should be interpreted as a plaintext character.

#+begin_example
,*** CLJS & Reframe vs Elm vs React + Redux
 - React + Redux quickly turns into spaghetti code
#+end_example
Here the =+= in the title causes a parse failure but should instead be interpreted as plaintext.

#+begin_example
,** SUSPENDED The suspended state causes an issue Foo + Bar
#+end_example
In this one there is a similar failure, however, it parses fine when the =SUSPENDED= state is either a recognizable state or missing.

#+begin_example
- State "CANCELLED"  from "TODO"       [2014-06-06 pią 08:26] \\
  -1 - this is not working right.
- State "CANCELLED"  from "TODO"       [2014-06-05 czw 15:31]
#+end_example
In this one there's no =+= character, but the =-1= is mistaken for a potential list item. It sholud be interpreted as a plaintext item (no space after the =-= character).

Please add test cases for all of these edge cases and fix them.

** CANCELLED Explicit indent in paragraphs
:LOGBOOK:
- State "CANCELLED"  from "TODO"       [2026-03-11 Wed 18:59] \\
  See comment.
:END:
Paragraph text now includes its indentation level. I'd like the indent to be explicitly parsed and added as the =indent= field of a paragraph node so that it's easier to recover the document structure at a later point.

The =indent= field should be explicitly bound in the paragraph. Corpus tests should be updated to support that field.

Add an additional corpus test that checks for paragraph indent parsing:

#+begin_example
,** Test
  hurr

- foo
  durr
#+end_example

Both =hurr= and =durr= containing paragraphs should have an indent.

*** COMMENT
The AI made some attempts but it proved to hard to do without confusing the indented paragraph for other elements. I proposed an alternative approach: having the indent as en explicit wrapper on all indent-sensitive elements, but Codex didn't like the idea.

** DONE Explicit indent blocks in sections
:LOGBOOK:
- State "DONE"       from "TODO"       [2026-03-11 Wed 19:07]
:END:
Are you certain that having a dedicated syntactic node for the indent wouldn't work? We could handle the current indent level in the scanner and just ignore the leading whitespace on the line when considering elements. Then the whole parse would be wrapped in a dedicated block effectively providing nesting.

Could the indent-block logic apply just to the sections? Zeroth section and heading sections.

I'd like to proceed with this idea, replace all indent handling in all elements with section-based indent-block handling. Each line's indent should be considered by the scanner to keep track of the current indent level. Elements should be parsed according to their usual rules, except without consideration for the indent (the scanner is taking care for that). The result should be a section content with explicit indent blocks that wrap the elements that share those indents. For elements, the initial indent at the element start should be the determining one (i.e. drawers can span a dedent).

The AST node for the indent should be called =block= and have an explicit field called =indent= that represents its indent level.
Blocks can be nested and they can nest other elements within them.

Execute the plan from @docs/plans/section-indent-block-plan.md The goal is 100% corpus test passing, do not omit the currently failing test.

I think your problems are stemming from the fact that blocks do not nest. Here's an example:

#+begin_example
- Foo
  Baz
  + hurr
    - derp
  + durr
- Bar
#+end_example

Following the =- Foo= line, there should be an indent-block of depth 2, following =+ hurr= there should be a single-line indent-block of depth 4.
There's no need to handle list nesting. Only blocks should nest. List's are no longer a thing. Only list items that are individual items that do not have a body even.

Indent blocks should be the only indent-bearing nodes in the AST. All other nodes shouldn't have anything to do with identation.
The per-element indent captures can be removed. Table row should also be handled without relying on the indent in the rule.
Blank line can remain an exception that consumes the whitespace.

Heading planning can consume its own whitespace (it's not part of a section or zeroth-section, so no need to handle that with indent blocks).

*** COMMENT
The brainstorming resulted in a plan stored for posterity in docs/plans. The AI extremely reluctant to implement any of it, stopping at every oportunity, explaining that this is a large refactor and that almost all tests would have to change.

A lot churning later it still couldn't do it. Took a good dozen extra encouragement prompts to get this somewhat working, but it was very hand-holdy.

** DONE Numeric priority values
:LOGBOOK:
- State "DONE"       from "TODO"       [2026-03-11 Wed 19:07]
:END:
Parser was updated not to recognize numeric priority values. Numeric values should be supported.

** DONE Post-refactor edge cases
I found some edge cases that don't seem to work now after the refactor:

#+begin_example
:properties:
:context: [[file:/ServiceWorker.scala::private%20def%20groupsToDtos(groupedSynced:%20Seq%5BSet%5BSynced%5D%5D,%20getSyncMapper:%20GetSyncMapper):%20SyncWorkerM%5BRegroupDtos%5D%20%3D%20{][file:/ServiceWorker.scala::private def groupsToDtos(groupedSynced: Seq[Set[Synced]], getSyncMapper: GetSyncMapper): SyncWorkerM[RegroupDtos] = {]]
:end:
#+end_example
Here, the link description fails to parse when in a drawer, however it parses fine outside of it.

#+begin_example
    #+PLOT: ind:1 deps:(2 3 4)
    |    10000 |     33634 |     53458 |     28740 |
    |    20000 |     66908 |    106692 |     61941 |
#+end_example
Indented keywords preceeding Org Tables seem to cause an issue in the parser. Mulitple PLOT lines complicate the issue even further. All of the above lines should be a parte of an indent block and parse as a single keyword line and an two-row Org table.

#+begin_example
,* DELEGATED Fix Nexus @types resolving.
#+end_example
The =@= character in the title stopped working. It should be treated as a regular plain text.

#+begin_example
Test
=============
Test
#+end_example
Here all lines should be treated as plain-text.

#+begin_example
:properties:
:context: [[file:~/events.cljs::{:dispatch [:fetch-docs]}))]]
:end:
#+end_example
Here's another link issue - the =:= causes a parse error. This is a valid link (I think).

#+begin_example
:properties:
:context: [[file:~/md.clj::p #"\n(?i)<!--\s*more\s*-->\n"] ;; FIXME Still needs contextual preview marker replacement.]]
:end:
#+end_example
Here's another issue - the link is malformed - it lacks an opening bracket on the link description. In such a case it would be ideal if the entire property value were parsed as plaintext.

#+begin_example
:properties:
:context: [[file:~/org/repeat.org::*Hangul romanization: [[jo/yo\][medial ㅛ]\]][Hangul romanization: medial ㅛ]]
:end:
#+end_example
This one uses a search term with brackets and escape sequence. It should fall back to a plaintext value.

#+begin_example
:properties:
:context: [[file:/StreamProvider.scala::textMessage.textStream.runFold("")(_ + _).map(_.parseJson.convertTo[StreamRequest])]]
:end:
#+end_example
Another scala link that doesn't parse well. The search term looks valid, it fails to parse as it uses brackets.

#+begin_example
```yaml
---
name: Load average
identifier: urn:example:load-average
```
#+end_example
Here the =---= line is reporting an error - that should be parsed as a text line.

#+begin_example
:properties:
:context: [[file:~/org/repeat.org::*\[\[Nobody can deny, that...\]\[Niemand kann leugnen, dass...\]\]][Niemand kann leugnen, dass...]]
:end:
#+end_example
Here the backslash character presents an issue in the search path.

#+begin_example
:LOGBOOK:  <
- State "DONE"       from "TODO"       [2017-03-17 Fri 20:42]
:END:      <<<<<<<<<<<<<<<<
#+end_example
This one is weird - the drawer start text parses fine, but then drawer parsing failse because of the additional angle brackets followig the =:end:=. I think the text following the =:end:= marker should be parsed as plaintext.

#+begin_example
```json
{
  "nodes": [{
    "parameters": [{
    }],
    "returnTypes": ["MONITOR_HEALTH_STATES"],
    "script": "...",
  }],
  "timestamp": "2022-01-17T16:17:55.707Z[Etc/UTC]"
}
```
#+end_example
Here's one more case that's failing. It's raw JSON in the body of the task. Specifically the =}],= is tripping up the parser. It should be treated as a plaintext in this case.

#+begin_example
  -
#+end_example
Here an issue stems from a single dash on a line - it's not a proper list item, so it should be treated as a plain text character.

#+begin_example
:properties:
:context: [[file:~/org/refile.org::DONE Add mDNS support to the SSM scala agent. :Scala:Networking:
:end:
#+end_example
Here, the end of the link syntaxt never appears, so the parser fails. As a fallback, this should be treated as a plaintext value.

#+begin_example
:LOGBOOK:
:LOGBOOK:
- State "DONE"       from "TODO"       [2026-03-11 Wed 21:11]
:END:
- State "DONE"       from "TODO"       [2023-04-16 nie 21:32]
- State "DONE" from "TODO" \[2023-04-12 śro 00:36\]
:END:
#+end_example
The second line should consist of plaintext - the incorrect timestamp should fail parsing and fallback to text.

#+begin_example
`foo << 2 + bar << 4 + baz << 8` is not the same as `(foo << 2) + (bar << 4) + (baz << 8)`.
#+end_example
Here, the text fails to parse as a target and should fallback to plaintext.

** DONE Logbook contents should recognize indent blocks
#+begin_example
:LOGBOOK:
:LOGBOOK:
- State "DONE"       from "TODO"       [2026-03-20 Fri 21:50]
:END:
- State "CANCELLED"  from "TODO"       [2025-07-28 pon 18:41] \\
  No need to.
- State "DONE"       from "TODO"       [2025-07-28 pon 18:33]
- State "DONE"       from "TODO"       [2025-07-26 sob 22:18]
- State "DONE"       from "TODO"       [2025-06-29 nie 20:54]
- State "DONE"       from "TODO"       [2025-06-26 czw 22:39]
- State "CANCELLED"  from "TODO"       [2025-06-20 pią 19:54] \\
  Missed it.
- State "CANCELLED"  from "TODO"       [2025-06-20 pią 19:54] \\
  Note
:END:
#+end_example

None of the indented lines are in a block, but they should be so that we can recover the list structure within a drawer element.
When this same list is outside of the drawer, it correctly parses with indent blocks.

There is one failing Python wrapper test that should pass after this is fixed on the grammar level.

** DONE Custom drawer text contents should always be wrapped in a paragraph
:LOGBOOK:
- State "DONE"       from "TODO"       [2026-03-20 Fri 22:13]
:END:
#+begin_example
  :test:
  [2026-01-01 Wto 10:00] This is not wrapped in a paragraph, unless dedented.
  :end:
#+end_example

The above fragment doesn't parse correctly - the contents are not wrapped in a paragraph when the entire drawer is indented. It parses correctly when the drawer itself is dedented.
Please make sure these indented drawers still parse correctly with a paragraph inside.

** DONE Indented timestamp issue
:LOGBOOK:
- State "DONE"       from "TODO"       [2026-03-20 Fri 22:13]
:END:
Here are some corner cases:

#+begin_example
:PROGRESS:
  [2025-01-06 Mon] 12,400 documents collected. Deduplication in progress.
:END:

  [2025-01-06 Mon] 12,400 documents collected. Deduplication in progress.

[2025-01-06 Mon] 12,400 documents collected. Deduplication in progress.
#+end_example

Both of the indented lines that start with a timestamp are failing to parse. The last line parses fine when there is no indent present. All lines parse fine if the indented lines have anything else than a timestamp at the very start (even if there's a timestamp following the prefix).

All of the above lines containing timestamps should parse as a paragraph of text that begins with a timestamp and is followed by some plain text.

Update the corpus tests that currently expect an ERROR node to be produced for these cases.

** DONE Multiple indentend timestamps
:LOGBOOK:
- State "DONE"       from "TODO"       [2026-03-20 Fri 22:53]
:END:
I found one more edge case:
#+begin_example
  :FEEDING_LOG:
  [2025-01-04 Sat] Fed 1:1:1 (starter:flour:water). Rose 3× in 4 hours.
  [2025-01-05 Sun] Fed 1:2:2. Peak at 6 hours — slightly slower.
  [2025-01-06 Mon] Fed 1:5:5. Testing for long cold ferment.
  :END:
#+end_example
All three lines should parse as paragraphs.

** DONE Another multi-line link edge case
:LOGBOOK:
- State "DONE"       from "TODO"       [2026-03-21 Sat 00:09]
:END:
#+begin_example
:PROPERTIES:
:context: [[file:src/org_parser/text/_rich_text.py::@classmethod
 def from_node(
 cls,
 node: tree_sitter.Node,
 *,
 document: Document,
 ) -> RichText:
 """Create a :class:`RichText` from a single tree-sitter node.]]
:END:
#+end_example

The above fails to parse correctly, the link should be a valid multi-line link.

** DONE Lone end marker breaks the parser
:LOGBOOK:
- State "DONE"       from "TODO"       [2026-03-20 Fri 23:59]
:END:
#+begin_example
,* TODO Heading
  - State "DONE"       from "TODO"       [2014-08-15 pią 23:25]
  :END:
#+end_example

The above example is technically malformed, but it should parse as a heading with one list item followed by a indented plaintext item.

The recent fix causes the END marker to not be a part of the indent block. Can this be fixed?

** DONE List bullet
:LOGBOOK:
- State "DONE"       from "TODO"       [2026-03-21 Sat 12:02]
:END:
Bind the =list_item= bullet in the parse tree - currently the various unordered list items are indistinguishable since they don't expose the list bullet in the parse tree.

Upate the list recovery to take the bullet into account: if a consecutive list item run encounters an item with a different bullet than the items so far, it should end the list run and start a new one.

Here's an example:
#+begin_example
- Foo
- Bar
  1. Ordered
  2. orderede
  a) another
  b) one
+ Baz
+ Faz
 * Hurr
 * Durr
 - Back
 - To dashes
#+end_example

The above example contains two, two-item top-level lists. Each of these lists' second item contains two nested lists that are two-items each.

The tree sitter parser should bind the bullet for all of these items.

The Python wrapper should use the bullet to recover the list structure.

** DONE Support for Babel call syntax
:LOGBOOK:
- State "DONE"       from "TODO"       [2026-03-21 Sat 21:49]
:END:
Add support for the Babel call syntax, both top-level and inline. It would be ideal to extract the call name an arguments and make them available.

You can find their syntax description in @docs/plans/syntax.md

These should be supported on the Tree-Sitter level with dedicated semantic nodes in the Python wrapper.

Please add Python wrapper nodes for the recently added Babel primitives.

** DONE Support for Macros
:LOGBOOK:
- State "DONE"       from "TODO"       [2026-03-21 Sat 22:11]
:END:
Add support for the inline macro call syntax. It suffices if the content within the braces is captured as text. No need to parse it further.

You can find their syntax description in @docs/plans/syntax.md

These should be supported on the Tree-Sitter level with dedicated semantic nodes in the Python wrapper.

** DONE Support for Org entities
:LOGBOOK:
- State "DONE"       from "TODO"       [2026-03-21 Sat 23:13]
:END:
Add support for the inline Org entities syntax. The values don't need to have individual semantic representation. It suffices to have a one =InlineEntity= class representing all of them.

You can find their syntax description in @docs/plans/syntax.md

These should be supported on the Tree-Sitter level with dedicated semantic nodes in the Python wrapper.

** DONE Support for subscript/superscript
:LOGBOOK:
- State "DONE"       from "TODO"       [2026-03-21 Sat 23:29]
:END:
Add support for the subscript and superscript inline syntax. The contents of the sub/superscript should be parsed according to the normal rules and be wrapped in an inline object representing the sub/superscript.

You can find their syntax description in @docs/plans/syntax.md

These should be supported on the Tree-Sitter level with dedicated semantic nodes in the Python wrapper.

** CANCELLED Support for LaTeX
:LOGBOOK:
- State "CANCELLED"  from "TODO"       [2026-03-21 Sat 22:44] \\
  These should be within a latex export block.
:END:
Add support for the LaTeX fragments, both inline and environments. The contents don't need to be parsed and can be represented as text in the parse tree.

You can find their syntax description in @docs/plans/syntax.md

These should be supported on the Tree-Sitter level with dedicated semantic nodes in the Python wrapper.

** DONE Better support for tableel tables
:LOGBOOK:
- State "DONE"       from "TODO"       [2026-03-21 Sat 23:55]
:END:
Table.el tables are parsed weird right now - just the frame lines are parsed as tableel tables with the actual table data parsed as Org Tables.

Please update the grammar to handle tableel tables correctly - the entire block should be parsed as verbatim text, i.e. no distinction for different rows or cells.

#+begin_example
+-----+-----+
| foo | bar |
+-----+-----+
| faz | baz |
+-----+-----+
#+end_example

This example should parse as a single table.el table, and not extract any structure.
Please add support for these degenerate tables in the Python wrapper. Split the Table.el semantic node into a separate class and make the current =Table= only handle proper org tables.

** DONE Code review comments
:LOGBOOK:
- State "DONE"       from "TODO"       [2026-03-22 Sun 00:43]
:END:
Fix the following issues:

- **High:** =tableel_table= regex in =tree-sitter-org/grammar.js= can over-consume following =+ ...= lines (e.g., list items) into the table node.

  - **Medium:** scanner prefix detection for inline =call_...= / =src_...= in =tree-sitter-org/src/scanner.c= can emit start tokens too early, causing parse errors for plain text that only starts with those prefixes but does not have valid inline syntax.

** DONE Indent parse regression
It seems that a recent fix introduced a regression:
#+begin_example
  :LOGBOOK:
:LOGBOOK:
- State "DONE"       from "TODO"       [2026-03-22 Sun 01:03]
:END:
  - State "CANCELLED"  from "TODO"       [2025-03-14 pią 13:11] \\
    Misse dit.
  :END:
  
#+end_example

Note the trailing line with spaces in it - the removal of this whitespace makes the parser work properly. This case should be handled and the blank line should be treated as part of the indent block.

* DONE Semantic nodes dev experience
:LOGBOOK:
- State "DONE"       from "TODO"       [2026-03-22 Sun 22:22]
:END:
** DONE Recover inherited tags
:LOGBOOK:
- State "DONE"       from "TODO"       [2026-03-22 Sun 15:40]
:END:
The =Heading.tags= property should also include the tags from the parent headings (if any). An additional attribute should be introduced called =heading_tags= that only returns the list of tags found on this particular heading (if any).

The =heading_tags= property should be mutable, while the =tags= should be made read-only.

In =tags=, duplicated tag entries should be removed (leave just the first occurance, exact match). The order should be preserved.

Add support for document-level =#+FILETAGS:= - a field called =tags= should be added to the =Document= class that returns a list of tags parsed from =#+FILETAGS:= keyword (if any). The =Document.tags= field should be mutable (adjusting the filetags keyword on set). These file-tags should be taken into account when computing the inherited tags property.

*** DONE src/org_parser/document/_heading.py
:PROPERTIES:
:context: 
:END:
:LOGBOOK:
- State "DONE"       from "TODO"       [2026-03-22 Sun 15:40]
:END:

In file src/org_parser/document/_heading.py around line 323, near the ~class Heading:~

#+begin_example
+        ancestors: list[Heading] = []
+        node: Heading | Document = self._parent
+        while isinstance(node, Heading):
+            ancestors.append(node)
+            node = node._parent
+
+        collected: list[str] = []
+        # Document FILETAGS come first.
+        collected.extend(node.tags)
+        # Ancestor tags outermost → innermost.
+        for ancestor in reversed(ancestors):
+            collected.extend(ancestor.heading_tags)
+        # Own tags last.
+        collected.extend(self._heading_tags)
+
+        # Deduplicate preserving first occurrence.
+        seen: set[str] = set()
+        result: list[str] = []
+        for tag in collected:
+            if tag not in seen:
+                seen.add(tag)
+                result.append(tag)
+        return result
#+end_example

It might be easier to just fetch the =parent.tags= and deduplicate making sure that the parent tags come first.

** DONE Keyword refactor
:LOGBOOK:
- State "DONE"       from "TODO"       [2026-03-22 Sun 16:04]
:END:
Update the =Document.keywords= field to be a list instead of a map.

All the fields that return a =Keyword= should instead return the value of the keyword. They still should have setters that allow setting the specific keyword value. The updated value should be reflected in the list returned by =keywords=.

** DONE Recover inherited categories
:LOGBOOK:
- State "DONE"       from "TODO"       [2026-03-22 Sun 16:23]
:END:
Add a mutable field called =heading_category= to the =Heading= class that returns the heading category computed as the value of the =CATEGORY= property from the =PROPERTIES= drawer (if available). When set, the relevant property should be added to the properties of this heading.

Add a read-only field called =category= to the =Heading= class that returns either the =heading_category= or an inherited =parent.category= if heading category is not available.

The document's own category should be extracted from the =#+CATEGORY= keyword (as done currently), or from the basename of the file the document was created from if no category is specified. If there's no file, there should be no category (=None=).

** DONE Attach CAPTION, TBLNAME, PLOT and RESULTS to the following element
:LOGBOOK:
- State "DONE"       from "TODO"       [2026-03-22 Sun 16:49]
:END:
Add an optional =keywords: list[Keyword] | None= field to the =Element= class that will be used to attach keywords to a an element. Add a read-only attribute =keywords= that returns the list.

Add a method called =attach_keyword= to the =Element= class - the metod should add a keyword to the internal keyword list. It should not mark the element as dirty.

Add a processing step to all =from_node= implementations that extract bodies that will attach any affilatable keywords such as CAPTION, TBLNAME, PLOT or RESULTS to the following elements.

The keywords should not be removed from the body, just "linked" to the following elements. If there is no following element available, then skip attaching the keyword.

The internal keywords should not be taken into account when stringifying elements.

** DONE Mutation handling
:LOGBOOK:
- State "DONE"       from "TODO"       [2026-03-22 Sun 17:11]
:END:
When an element is attached to another element their =parent= relation and dirty flag should be set correctly. Make sure that is the case in every setter.

If a heading is attached to another heading, its level should be adjusted to be at least one-more than the parent. All of its children's levels should be adjusted as well. All adjusted headings should be marked dirty.

** DONE Clock extraction
:LOGBOOK:
- State "DONE"       from "TODO"       [2026-03-22 Sun 17:34]
:END:
Update the =Heading= class with a new field =clock_entries= - returns a list of all =CLOCK= items (by analogy to =repeated_tasks=).

The extraction of these values should be done in a similar fashion to the =repeated_tasks= extraction. The goal is for the heading to cache the clocked items.

The body of the =Logbook= should be updated with the recovered =Clock= and =Repeat= values - fetching the logbook body and the list of repeats should result in the same objects to be present in the list. Same for clocks.

** DONE More repeats/clocks recovery
:LOGBOOK:
- State "DONE"       from "TODO"       [2026-03-22 Sun 17:57]
:END:
Check all heading body lists for potential repeats.

Similarly, all CLOCK items within a body of a heading should be extracted and stored in the heading =clock_entries= property.

*** DONE src/org_parser/document/_heading.py
:PROPERTIES:
:context: 
:END:
:LOGBOOK:
- State "DONE"       from "TODO"       [2026-03-22 Sun 17:55]
:END:

In file src/org_parser/document/_heading.py around line 868, near the ~def _find_first_subheading(node: tree_sitter.Node) -> tree_sitter.Node | None:~

#+begin_example
+                    walk(converted_item.body)
#+end_example

Don't descend into the list bodies, only top-level lists should be analyzed for heading repeats/clocks.
*** DONE src/org_parser/document/_heading.py
:PROPERTIES:
:context: 
:END:
:LOGBOOK:
- State "DONE"       from "TODO"       [2026-03-22 Sun 17:57]
:END:

In file src/org_parser/document/_heading.py around line 879, near the ~def _find_first_subheading(node: tree_sitter.Node) -> tree_sitter.Node | None:~

#+begin_example
+def _nested_body_elements(element: Element) -> list[Element] | None:
+    """Return nested element body list when *element* exposes one.
+
+    Some semantic element classes use a ``body`` attribute that is itself a
+    list of nested :class:`Element` instances (for example drawers, container
+    blocks, and list items). This helper returns that nested list when present
+    and type-compatible, otherwise ``None``.
+    """
+    body = getattr(element, "body", None)
+    if not isinstance(body, list):
+        return None
+    if any(not isinstance(nested, Element) for nested in body):
+        return None
+    return body
#+end_example

I don't like this approach - please update the recovery algorithm to explicitly mention elements by class name. We expect CLOCK entries and repeats only in LOGBOOK drawers, custom drawers and lists.

** DONE Convenience fields
:LOGBOOK:
- State "DONE"       from "TODO"       [2026-03-22 Sun 19:04]
:END:
Add extra read-only fields for convevience:

1. =Document=:
   - =is_root= - returns =True=
   - =is_leaf= - returns =True= if the =children= list is empty.
   - =all_states= - returns all discovered valid TODO states (based on the =#+TODO:= keyword).
   - =todo_states= - returns all discovered valid not-completed states (based on the =#+TODO:= keyword).
   - =done_states= - returns all discovered valid completed states (based on the =#+TODO:= keyword).
   - =body_text= - returns the stringified text of the =body=.
   - =all_headings= - returns the list of all headings of any level (children, and their children) in file-definition order.

2. =Heading=:
   - =is_root= - returns =False= if the heading has a =parent=.
   - =is_leaf= - returns =True= if the =children= list is empty.
   - =is_completed= - returns =True= if the current TODO state is within the =documen.done_states=.
   - =has_timestamp= - returns =True= if any of the timestamps are set or if there are repeated tasks.
   - =timestamps= - returns a list of all timestamps of this task, including : =scheduled=, =closed=, =deadline=, any =repeated_tasks= or any =clock_entries=.
   - =latest_timestamp= - returns the latest timestamp from the set: =scheduled=, =closed=, =deadline=, any =repeated_tasks= or any =clock_entries=.
   - =earliest_timestamp= - return the earliest timestamp from the set: =scheduled=, =closed=, =deadline=, any =repeated_tasks= or any =clock_entries=.
   - =body_text= - returns the stringified text of the =body=.
   - =title_text= - returns the stringified text of the =title=.
   - =heading_text= - returns the stringified text of the entire heading line, including the stars, priority, todo state, etc.

3. =Element= subclasses:
   - =text= - returns the stringified text of the element (=return str(self)=).
   - =body_text= - returns the stringified text of the =body= attribute (if the element subclass has one).

*** DONE src/org_parser/document/_heading.py
:PROPERTIES:
:context: 
:END:
:LOGBOOK:
- State "DONE"       from "TODO"       [2026-03-22 Sun 18:45]
:END:

In file src/org_parser/document/_heading.py around line 488, near the ~class Heading:~

#+begin_example
+def _ensure_document_child_heading_level(child: Heading, *, parent_level: int) -> None:
+    """Ensure one top-level child heading has a valid level for *parent_level*."""
+    min_level = parent_level + 1
+    if child.level >= min_level:
+        return
+    delta = min_level - child.level
+    _shift_document_heading_subtree(child, delta=delta)
+
+
+def _shift_document_heading_subtree(heading: Heading, *, delta: int) -> None:
+    """Increase one heading subtree's levels by *delta*."""
+    heading.level = heading.level + delta
+    for child in heading.children:
+        _shift_document_heading_subtree(child, delta=delta)
+
+
 def _render_document_dirty(document: Document) -> str:
     """Render a dirty document from semantic fields only."""
     parts: list[str] = []
modified   src/org_parser/document/_heading.py
@@ -486,6 +486,74 @@ class Heading:
             _ensure_child_heading_level(child, parent_level=self._level)
         self._mark_dirty()
#+end_example

This is a direct copy of the heading functions. Please make those ones public and re-use them here.
*** DONE src/org_parser/document/_heading.py
:PROPERTIES:
:context: 
:END:
:LOGBOOK:
- State "DONE"       from "TODO"       [2026-03-22 Sun 18:49]
:END:

In file src/org_parser/document/_heading.py around line 523, near the ~class Heading:~

#+begin_example
+    @property
+    def latest_timestamp(self) -> Timestamp | None:
+        """Latest timestamp across planning values and logbook-derived timestamps."""
+        values = self.timestamps
+        if not values:
+            return None
+        return max(values, key=lambda timestamp: timestamp.start)
#+end_example

Should use =timestamp.end= if provided and fallback to start if not.
*** DONE src/org_parser/element/_element.py
:PROPERTIES:
:context: 
:END:
:LOGBOOK:
- State "DONE"       from "TODO"       [2026-03-22 Sun 18:56]
:END:

In file src/org_parser/element/_element.py around line 90, near the ~class Element:~

#+begin_example
+    @property
+    def body_text(self) -> str:
+        """Stringified text of this element's ``body`` value when present."""
+        body: object | None = getattr(self, "body", None)
+        if body is None:
+            return ""
+        if isinstance(body, list):
+            body_list = cast("list[object]", body)
+            return "".join(str(element) for element in body_list)
+        return str(body)
#+end_example

That's lazy, please add individual methods to the elements that have a body.
*** DONE src/org_parser/document/_document.py
:PROPERTIES:
:context: 
:END:
:LOGBOOK:
- State "DONE"       from "TODO"       [2026-03-22 Sun 19:03]
:END:

In file src/org_parser/document/_document.py around line 740, near the ~def _append_heading_subtree(headings: Sequence[Heading], parts: list[str]) -> No~

#+begin_example
+def _parse_todo_states(todo: RichText | None) -> tuple[list[str], list[str], list[str]]:
+    """Return ``(all_states, todo_states, done_states)`` for ``#+TODO:`` text."""
+    if todo is None:
+        return [], [], []
+
+    tokens = [token for token in str(todo).split() if token != ""]
+    if not tokens:
+        return [], [], []
+
+    if "|" not in tokens:
+        return tokens, tokens, []
+
+    split_index = tokens.index("|")
+    todo_states = [token for token in tokens[:split_index] if token != "|"]
+    done_states = [token for token in tokens[split_index + 1 :] if token != "|"]
+    all_states = [*todo_states, *done_states]
+    return all_states, todo_states, done_states
#+end_example

This needs to be tested with "complex" todo specifiers:

#+begin_example
#+TODO: TODO(t) | DONE(d@/!)
#+TODO: | CANCELLED(c@/!) REWORKED(r@/!)
#+end_example

Document should also handle multiple =#+TODO= keywords.
** DONE Iterator/Sequence interface
:LOGBOOK:
- State "DONE"       from "TODO"       [2026-03-22 Sun 19:33]
:END:
=Document=, =Heading=, =Table=, =List=, =Drawer=, =Logbook=, =Properties= and  =RichText= should support an iterator/sequence interface:

- =Document= iterates over all the headings contained within, including subheading (=all_headings= attribute).
- =Heading= iterates over the children
- =Table= iterates over the rows, which iterate over the colum values. It should be possible to mutate table cell values via assignment: ~table[0][0] = value~
- =List= iterates over the list items.
- =RichText= iterates over constituent parts.
- =Drawer= iterates over the body elements.
- =Logbook= iterates over the recovered body elements.
- Any other elements that return a list as their body, should iterate over the body.
- =Properties= should be indexable by strings and return the values of their properties. It should be possible to mutate =Properties= via setting a key ~node.properties["foo"] = bar~.

Example: =document[0][2].body[2][x][y]= - x,y table cell in a body of third child of the first heading in a document.

** DONE Convenience constructors
:LOGBOOK:
- State "DONE"       from "TODO"       [2026-03-22 Sun 22:13]
:END:
Add a new class method =from_source= to =Document=, =Heading=,  =RichText=, =Timestamp= and =Element= subclasses that will invoke the parser and extract the resulting parse tree as a semantic node. These new methods will be used to:

1. Programatically create RichText from strings
   #+begin_example
   RichText.from_source("*this* should /parse/ fine_{+1}")
   #+end_example

2. Programatically create timestamps from strings
   #+begin_example
   Timestamp.from_source("<2026-03-22 Sun 14:43>")
   #+end_example

3. Programatically create arbitrary elements
   #+begin_example
   List.from_source("- foo\n- bar\n")
   #+end_example

4. Programatically create arbitrary headings
   #+begin_example
   Heading.from_source("* TODO Some new task")
   #+end_example

5. Programatically create entire documents
   #+begin_example
   Document.from_source("#+title: Document\n* TODO Some task")
   #+end_example

In most cases, this should just be equivalent to something similar to:
#+begin_src python
tree = PARSER.parse(source)
doc = Document.from_tree(tree)
return cls.from_node(extract_specific_node(tree), doc)
#+end_src

If the parse tree has errors or if it results in an unexpected tree structure, the method should raise an =ValueError=.

Since most of these =from_source= implementations will be very similar, it makes sense to abstract the parsing into a helper function that runs the parser and accepts a node extractor returning a =node= and =document=.

*** DONE Code review comment
:LOGBOOK:
- State "DONE"       from "TODO"       [2026-03-22 Sun 21:47]
:END:
:PROPERTIES:
:context: [[file:~/projects/software/org-mode-parser/src/org_parser/_from_source.py::if _tree_has_parse_errors(tree.root_node):
 raise ValueError("Source contains parse errors")]]
:END:

Filename: /home/k/projects/software/org-mode-parser/src/org_parser/_from_source.py

#+begin_example
    if _tree_has_parse_errors(tree.root_node):
        raise ValueError("Source contains parse errors")
#+end_example

You can use `document.errors` to see if there are any errors reported.
*** DONE src/org_parser/_from_source.py
:PROPERTIES:
:context: 
:END:
:LOGBOOK:
- State "DONE"       from "TODO"       [2026-03-22 Sun 22:11]
:END:

In file src/org_parser/_from_source.py around line 52, near the ~Merge~

#+begin_example
+def parse_source_with_extractor(
+    source: str,
+    *,
+    extractor: Callable[[tree_sitter.Node, Document], tree_sitter.Node | None],
+) -> tuple[tree_sitter.Node, Document]:

#+end_example

Does it make more sense to return the parsed element here? Now if we return the node we're re-doing the same parsing work as was already done within this function. I'd rater the result of calling =parse_source_with_extractor= to be the element and the document. You can use typevars for the extractor and return types.

The extractors should also use the document for the extraction, not the Tree-Sitter nodes.

*** DONE src/org_parser/text/_rich_text.py
:PROPERTIES:
:context: 
:END:
:LOGBOOK:
- State "DONE"       from "TODO"       [2026-03-22 Sun 22:13]
:END:

In file src/org_parser/text/_rich_text.py around line 219, near the ~class RichText:~

#+begin_example
+        if not isinstance(rich_text, cls):
+            raise ValueError("Unexpected parse tree structure")
#+end_example

This guard is not needed - we're passing the extractor that either extracts a value or causes =parse_source_with_extractor= to throw. Please remove these.
** DONE Code review comment
:PROPERTIES:
:context: 
:END:
:LOGBOOK:
- State "DONE"       from "TODO"       [2026-03-22 Sun 23:51]
:END:

- High: dirty document rendering can lose repeated dedicated keywords, especially multiple `#+TODO:` lines, because `_render_document_dirty` only keeps the first dedicated key then drops the rest (`src/org_parser/document/_document.py:809`, `src/org_parser/document/_document.py:816`).
- Medium: dedicated keyword lookup currently returns the first match (`src/org_parser/document/_document.py:509`) but the class docs say last-write-wins (`src/org_parser/document/_document.py:82`), so behavior and contract conflict.
- Medium: `Heading.tags` dedup is incomplete; duplicates inside `heading_tags` can leak through because `seen` is not updated during own-tag filtering (`src/org_parser/document/_heading.py:327`).

** DONE Code review comment
:LOGBOOK:
- State "DONE"       from "TODO"       [2026-03-23 Mon 00:03]
:END:
:PROPERTIES:
:context: [[file:~/projects/software/org-mode-parser/src/org_parser/document/_document.py::def _mark_dirty(self) -> None:
 """Mark this document as dirty."""
 if self._dirty:
 return
 self._dirty = True]]
:END:

Filename: /home/k/projects/software/org-mode-parser/src/org_parser/document/_document.py

#+begin_example
    def _mark_dirty(self) -> None:
        """Mark this document as dirty."""
        if self._dirty:
            return
        self._dirty = True
#+end_example

The private =_mark_dirty= method is pretty useless. We can go with only the publicly available =mark_dirty=. Please update all the classes that have a =_mark_dirty= and =mark_dirty= to only use the public one.
* DONE Tree-Sitter structure recovery
:LOGBOOK:
- State "DONE"       from "TODO"       [2026-03-24 Tue 18:37]
:END:
** DONE Support for list_item bodies
:LOGBOOK:
- State "DONE"       from "TODO"       [2026-03-23 Mon 18:24]
:END:
Make list items accept a block body. It should be enough to accept just one block as the body as the block will likely contain all the relevant bits already.

** DONE Anonymous body nodes
:LOGBOOK:
- State "DONE"       from "TODO"       [2026-03-23 Mon 18:24]
:END:
Make the list item body block node anonymous so that all the bits are attached to the list item.

** DONE Make list items be wrapped in a list node
:LOGBOOK:
- State "DONE"       from "TODO"       [2026-03-23 Mon 19:43]
:END:
Wrap multiple consecutive list items in a list node. The list node should accept multiple items that themselves can contain lists via the block bodies. The lists should expose the list items under the =item= property.

** DONE Block handling of lists & paragraphs
:LOGBOOK:
- State "DONE"       from "TODO"       [2026-03-23 Mon 22:45]
:END:
When there are lists within a indent block, they are parsed as separate single-item lists. Lists should not be terminated on newlines within blocks.

#+begin_example
- foo
  - baz
  - herp
- bar
#+end_example

In the above example, there should be two, two-item lists =list='s, one top-level and one nested in the first list item of the top-level list.

Similarly, multiple text lines within a block currently parse as multiple separate paragraphs. It would be nice if these could parse as one paragraph that spans the entire text in a block:

#+begin_example
  Text
  Text
  Text
#+end_example

In the above example, all three text lines should be a memeber of the same paragraph that itself is within a block.

There's still an issue with list items within a block when there's more than two list items - each two items are grouped together, instead of grouping as a single list:

#+begin_example
- foo
  - baz
  - herp
  - drep
  - hurr
  - durr
- bar
- baz
#+end_example

There are two lists here, one nested as the child of the first list item of the top-level list.

There's an edge case:

#+begin_example
- list
  - sublist
    bar
    foo
  - sublist
- list
#+end_example

When a list body contains a paragraph that spans multiple lines, it causes the list to terminate and the text encountered list item is wrapped in another list object.

** DONE Rename block to indent
:LOGBOOK:
- State "DONE"       from "TODO"       [2026-03-23 Mon 23:09]
:END:
Rename the =block= node to =indent= as it's more descriptive.

** DONE Indentation edge cases
:LOGBOOK:
    - State "DONE"       from "TODO"       [2026-03-24 Tue 00:32]
:END:
There's a small edge case when a drawer list item has a note and is misaligned:

#+begin_example
    :LOGBOOK:
- State "CANCELLED"  from "ACTIVE"     [2013-05-29 śro 09:03] \\
  No need to.
- State "ACTIVE"     from "TODO"       [2013-05-29 śro 09:02]
    :END:
#+end_example

This should parse fine.

Here's one more edge case:

#+begin_example
,* Test
    :LOGBOOK:
- State "CANCELLED"  from "TODO"       [2013-06-05 śro 14:39] \\
  Not a bug, really.
    :END:
#+end_example

When the list note is the final element, the drawer isn't parsed correctly.

** DONE Python list recovery
:LOGBOOK:
- State "DONE"       from "TODO"       [2026-03-24 Tue 00:55]
:END:
Update the Python wrapper not to have to recover list and paragraph structure but rather accept the Tree-Sitter node structure as is currently. Do keep the =Repeat= recovery though in place.
The =IndentBlock= should be renamed to just =Indent= and treated as a first-class citizen in the semantic tree.

** DONE Another indent edge case
:LOGBOOK:
- State "DONE"       from "TODO"       [2026-03-24 Tue 19:16]
:END:
The =h= character below causes an error. Any single-character, indented paragraph fails when it's a body of a nested list that has more items following it.

#+begin_example
- foo
  - baz
  h
  - foo
#+end_example

* Bugfixes
** DONE Comment headings
:LOGBOOK:
- State "DONE"       from "TODO"       [2026-03-25 Wed 14:14]
:END:
The =COMMENT= token in headings doesn't parse as the comment indicator but rather a plain text string

#+begin_example
,* COMMENT Title
,* [#A] COMMENT Title
,* [#A] COMMENT Title :tag:foo:
,* TODO [#A] COMMENT Title :tag:foo:
,* TODO [#A] COMMENT Title
,* TODO [#A] COMMENT
,* TODO COMMENT
,* COMMENT
#+end_example

All of the above should be marked as comment headings on the tree-sitter heading node.

Update the Python wrapper to also expect the comment indicator. The =Heading= class should have a mutable field =is_comment= that returns the comment indicator and is able to set it.

** DONE Tags on list items
:LOGBOOK:
- State "DONE"       from "TODO"       [2026-03-25 Wed 17:43]
:END:

List items can have an optional tag associated with them:

#+begin_example
- tag :: item contents
- longer tag :: item contents
+ [-] tag :: item contents
1. [@5] tag :: item contents
#+end_example

All of the above should parse with a leading tag.

*** DONE Regressions
:LOGBOOK:
- State "DONE"       from "TODO"       [2026-03-25 Wed 17:43]
:END:

It looks like text now splits around single =:= characters. This should work without issues, only =::= should be considered a list item tag separator.

There are numerous regressions because of that:

#+begin_example
- Item
  Test :hurr: durr
#+end_example

Here the collon causes an error in the list item body parsing. It's the same failure as here:

#+begin_example
:LOGBOOK:
   - State "CANCELLED"  from "TODO"       [2021-01-18 Mon 20:18] \\
     No need to. :(
:END:
#+end_example

** CANCELLED Lower-case CLOCK items.
:LOGBOOK:
- State "CANCELLED"  from              [2026-03-25 Wed 12:55] \\
  Org-Mode in Emacs doesn't recognize these even though it's documented in https://orgmode.org/worg/org-syntax.html
:END:
#+begin_example
clock: [2024-10-12]
#+end_example

* Error recovery overhaul
It would be nice to report parse errors for some of the obviously broken constructions.

- [X] unclosed markup should report a missing close on end of line
- [ ] misaligned drawer start/end markers
- [X] trailing bits after drawer start/end markers
- [ ] misaligned CLOCK
- [X] malformed CLOCK lines
- [ ] misalign PLANNING
- [X] malformed PLANNING
- [X] malformed timestamps
- [ ] misaligned block start/end markers
- [X] tailing bits after block start/end markers
- [X] malformed org tables
- [ ] malformed special keyword values

** CANCELLED Malformed inline markup should report a missing close on end of line
:LOGBOOK:
- State "CANCELLED"  from "TODO"       [2026-03-26 Thu 21:20] \\
  Proved to be too troublesome for genuinly useful cases (file paths, globs, etc).
:END:
Ideally, the parse tree would point out the missing closing markup.

#+begin_example
*not bold
 /not italic
not ~code
not strikethrough+
,* Not _underline
#+end_example

All of the above lines should contain an error.

** CANCELLED Trailing text after drawer start/end markers should be marked as errors
:LOGBOOK:
- State "CANCELLED"  from "TODO"       [2026-03-26 Thu 22:16] \\
  Didn't work very well, either full drawer is text, or special "semantic error" nodes need to be introduced.
:END:
Ideally, the error would be limited to just the trailing text.

#+begin_example
:LOGBOOK: <<<<<<<<<<<<
:END:
:PROPERTIES:
:END: >
#+end_example

In the above example, the two drawers should mostly parse fine with an error node attached to each around the trailing text.

** TODO Trailing text after list item line-break =\\= should be marked as error
Ideally, the item line would parse fine with a line break at the end and a trailing error node.

#+begin_example
:LOGBOOK:
- State "DONE" from "TODO" [2025-02-10] \\ not acceptable
  Note
:END:
#+end_example

In the above example the =not acceptable= string should be an error.

** TODO Malformed CLOCK lines should be marked as errors
Ideally, only the clock line would be marked as an error.

#+begin_example
CLOCK: => foo:bar
CLOCK: <2025-12-12
CLOCK: [2023-07-03 Mon 09:45]--<2023-07-03 Mon 14:10> =>  4:25
#+end_example

In the above example, each line should be marked as an error - first line due to the malformed duration, the remaining two because of the malformed timestamps.

** DONE Malformed planning lines should be marked as errors
:LOGBOOK:
- State "DONE"       from "TODO"       [2026-03-26 Thu 22:47]
:END:
Ideally, just the planning value that contains the error should be marked as such with the remaining, well-formed, planning items parsed fine.

#+begin_example
,* Test
SCHEDULED: hurr
DEADLINE: [2024-02-02]--<2025-02-02>
CLOSED: <
#+end_example

In the above example, the SCHEDULED time value is not a timestamp, hence should be marked as an error. The two remaining lines contain malformed timestamps and also should be marked as errors.

** TODO Trailing bits after block start/end markers should be marked as errors
Ideally, the block would still parse as a block, with the error node attached to it marking the trailing text.
#+begin_example
,#+begin_example
Not right
,#+end_example>
#+end_example

In the above example, the =>= should be marked as an error.
Ensure that blocks that do accept additional data on either line parse fine.

** TODO Malformed org-tables should be marked as errors
Tables that have a trailing text should result in an error on the parse tree. Leading text should cause the table not to parse as a table but plaintext.

#+begin_example
foo|bar|baz|

|foo | bar | baz
#+end_example

In the above example, the first line should be a plaintext paragraph, while the second should be marked as missing the =|= character.

* Release
** DONE Supported syntax in the README.org
:LOGBOOK:
- State "DONE"       from "TODO"       [2026-03-22 Sun 00:09]
:END:
Please expand the README.org file with examples of all the features supported by the parser. It should list all the supported features in the =Supported features= heading as subheadings.

Each features should have a clear example embedded within the file itself and a description of whether it is supported in Tree-Sitter grammar or just in Python wrapper and via what means (node names, module names etc).
Keep the examples short and instrumental, focused on just a single feature.
Keep the descriptions short and focused.

You can combine multiple related features into a single subheading (for instance, all inline markup objects can be a single subheading, various lists, etc). Here's an example:

#+begin_example
,** Inline markup
_This_ /parser/ *supports* =the= ~following~ +inline+ markup^{objects}.

All of the above are supported in the Tree-Sitter grammar as well as in the Python wrapper.
#+end_example

** Tree-Sitter parser pulled into a separate repository
** Semantic node documentation
** Library release
