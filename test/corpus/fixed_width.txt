================
Single fixed-width line
================
: single line

---

(document
  (zeroth_section
    (fixed_width
      (fixed_width_value))))

================
Empty fixed-width line (colon alone)
================
:

---

(document
  (zeroth_section
    (fixed_width)))

================
Multiple consecutive fixed-width lines coalesce into one node
================
: line one
: line two
: line three

---

(document
  (zeroth_section
    (fixed_width
      (fixed_width_value)
      (fixed_width_value)
      (fixed_width_value))))

================
Consecutive mixed value and empty fixed-width lines coalesce
================
: line one
:
: line three

---

(document
  (zeroth_section
    (fixed_width
      (fixed_width_value)
      (fixed_width_value))))

================
Blank line separates fixed-width blocks
================
: block one

: block two

---

(document
  (zeroth_section
    (fixed_width
      (fixed_width_value))
    (blank_line)
    (fixed_width
      (fixed_width_value))))

================
Indented consecutive fixed-width lines coalesce
================
   : line one
   : line two
   : line three

---

(document
  (zeroth_section
    (indent
      (whitespace)
      (fixed_width
        (fixed_width_value)
        (fixed_width_value)
        (fixed_width_value)))))

================
Indented fixed-width line
================
   : indented

---

(document
  (zeroth_section
    (indent
      (whitespace)
      (fixed_width
        (fixed_width_value)))))

================
Indented empty fixed-width line
================
   :

---

(document
  (zeroth_section
    (indent
      (whitespace)
      (fixed_width))))

================
Fixed-width followed by paragraph
================
: area line one
: area line two

This is a paragraph.

---

(document
  (zeroth_section
    (fixed_width
      (fixed_width_value)
      (fixed_width_value))
    (blank_line)
    (paragraph
      (plain_text)
      (newline))))

================
Double colon in paragraph is not fixed-width
================
test :: Not a fixed width section

---

(document
  (zeroth_section
    (paragraph
      (plain_text)
      (newline))))

================
Paragraph ending with colon is not fixed-width
================
Test paragraph:

---

(document
  (zeroth_section
    (paragraph
      (plain_text)
      (newline))))

================
Colon in middle of paragraph is not fixed-width
================
Another test: yes, it passes

---

(document
  (zeroth_section
    (paragraph
      (plain_text)
      (newline))))
